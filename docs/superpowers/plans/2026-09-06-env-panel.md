# 环境管理面板 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Web UI 用活动栏第三项完整管理可 git 共享的环境模板（新建 / 编辑变量 / 改名 / 删除），树选中只编辑，顶栏选择器才是发送环境。

**Architecture:** `workspace` 新增 `DeleteEnvironment` 与 `RenameEnvironment`，删/改当前环境时服务端写 `active.yaml`（失败则回滚文件）。HTTP 增加 `DELETE /api/environments/{name}` 与 `POST /api/environments/{name}/rename`。前端 `LeftView` 增加 `"environment"`，主栏换成 `EnvEditor`；密钥面板与 `PUT` upsert 不变。

**Tech Stack:** Go 1.22；React + TypeScript + Vite + 手写 CSS；Vitest；无新 npm / Go 依赖。

## Global Constraints

- 规格：`docs/superpowers/specs/2026-09-06-env-panel-design.md`。
- 环境名 = `environments/` 下不含 `.yaml` 的文件名，不能含 `/`，规则与现有 `envPath` / `CleanRel` 相同。
- YAML 仍是 `name` + `variables` 字符串 map；`.httptest/local/secrets.yaml` 与顶栏密钥面板不改。
- `PUT /api/environments/{name}` 保持 upsert；新建同名保护只在 UI。
- 不新增 npm 包；不加载 Web 字体；界面中文。
- 本次不做：未保存切换拦截、环境复制、从密钥导入、环境子目录、覆盖已存在改名目标、密钥面板重做。
- 不回头改写历史规格或计划文件。

## File Structure

| 路径 | 职责 |
|------|------|
| `internal/workspace/env.go` | `ErrEnvExists`、`ErrSameEnvName`、`writeActive`、`DeleteEnvironment`、`RenameEnvironment` |
| `internal/workspace/env_test.go` | 删除 / 改名 / 回滚 / 冲突 |
| `internal/server/server.go` | 注册 DELETE 与 POST rename |
| `internal/server/handlers.go` | handler；`writePathErr` 映射 409 与 same-name 400 |
| `internal/server/handlers_test.go` | HTTP 200/204/400/404/409 |
| `web/src/activity.ts` | `LeftView` 增加 `"environment"` |
| `web/src/activity.test.ts` | 三图标状态机 |
| `web/src/env.ts` | 键值对、脏标记、环境名校验、打开编辑草稿 |
| `web/src/env.test.ts` | 脏标记、同名键、非法名、树选中不改 local |
| `web/src/api.ts` | `deleteEnvironment`、`renameEnvironment` |
| `web/src/Dialog.tsx` | path 对话框可配 `hint`；intent / confirm subject 扩展 |
| `web/src/EnvEditor.tsx` | 环境变量编辑器 |
| `web/src/ActivityBar.tsx` | 第三图标 |
| `web/src/Sidebar.tsx` | 环境列表 |
| `web/src/App.tsx` | 视图切换、CRUD、快捷键 |
| `web/src/styles.css` | 环境编辑器与「发送」标记 |
| `README.md` | 「界面」一节 |

不改：`internal/executor/`、`cmd/`、密钥面板行为、请求 YAML schema。

---

### Task 1: workspace 删除环境

**Files:**
- Modify: `internal/workspace/env.go`
- Modify: `internal/workspace/env_test.go`

**Interfaces:**
- Consumes: `envPath`、`GetLocal`、`PutLocal` 现有行为
- Produces:
  - `func (w *Workspace) writeActive(environment string) error`（包内）
  - `func (w *Workspace) DeleteEnvironment(name string) error`
  - 删除当前环境时 `active.yaml` 的 `environment` 为空字符串；不存在返回 `os.ErrNotExist`；非法名返回现有 `invalid environment name` 错误

- [ ] **Step 1: 在 `env_test.go` 末尾追加失败测试**

```go
func TestDeleteEnvironmentRemovesFileAndClearsActive(t *testing.T) {
	ws, err := Init(t.TempDir())
	if err != nil {
		t.Fatal(err)
	}
	if err := ws.PutEnvironment(Environment{Name: "local", Variables: map[string]string{"k": "v"}}); err != nil {
		t.Fatal(err)
	}
	if err := ws.PutLocal(Local{Environment: "local", Secrets: map[string]string{"t": "1"}}); err != nil {
		t.Fatal(err)
	}
	if err := ws.DeleteEnvironment("local"); err != nil {
		t.Fatal(err)
	}
	if _, err := os.Stat(filepath.Join(ws.Workdir(), "environments", "local.yaml")); !os.IsNotExist(err) {
		t.Fatalf("file still there: %v", err)
	}
	got, err := ws.GetLocal()
	if err != nil {
		t.Fatal(err)
	}
	if got.Environment != "" {
		t.Fatalf("active=%q want empty", got.Environment)
	}
	if got.Secrets["t"] != "1" {
		t.Fatalf("secrets mutated: %+v", got.Secrets)
	}
}

func TestDeleteEnvironmentLeavesOtherActive(t *testing.T) {
	ws, err := Init(t.TempDir())
	if err != nil {
		t.Fatal(err)
	}
	if err := ws.PutEnvironment(Environment{Name: "local", Variables: map[string]string{}}); err != nil {
		t.Fatal(err)
	}
	if err := ws.PutEnvironment(Environment{Name: "prod", Variables: map[string]string{}}); err != nil {
		t.Fatal(err)
	}
	if err := ws.PutLocal(Local{Environment: "local", Secrets: map[string]string{}}); err != nil {
		t.Fatal(err)
	}
	if err := ws.DeleteEnvironment("prod"); err != nil {
		t.Fatal(err)
	}
	got, err := ws.GetLocal()
	if err != nil {
		t.Fatal(err)
	}
	if got.Environment != "local" {
		t.Fatalf("active=%q", got.Environment)
	}
}

func TestDeleteEnvironmentMissing(t *testing.T) {
	ws, err := Init(t.TempDir())
	if err != nil {
		t.Fatal(err)
	}
	err = ws.DeleteEnvironment("nope")
	if !os.IsNotExist(err) {
		t.Fatalf("got %v", err)
	}
}

func TestDeleteEnvironmentInvalidName(t *testing.T) {
	ws, err := Init(t.TempDir())
	if err != nil {
		t.Fatal(err)
	}
	err = ws.DeleteEnvironment("a/b")
	if err == nil || !strings.Contains(err.Error(), "invalid environment name") {
		t.Fatalf("got %v", err)
	}
}

func TestDeleteEnvironmentRollsBackFileWhenActiveWriteFails(t *testing.T) {
	ws, err := Init(t.TempDir())
	if err != nil {
		t.Fatal(err)
	}
	if err := ws.PutEnvironment(Environment{Name: "local", Variables: map[string]string{"k": "v"}}); err != nil {
		t.Fatal(err)
	}
	if err := ws.PutLocal(Local{Environment: "local", Secrets: map[string]string{}}); err != nil {
		t.Fatal(err)
	}
	active := filepath.Join(ws.LocalDir(), "local", "active.yaml")
	if err := os.Remove(active); err != nil {
		t.Fatal(err)
	}
	if err := os.Mkdir(active, 0o755); err != nil {
		t.Fatal(err)
	}
	if err := ws.DeleteEnvironment("local"); err == nil {
		t.Fatal("expected error")
	}
	if _, err := os.Stat(filepath.Join(ws.Workdir(), "environments", "local.yaml")); err != nil {
		t.Fatalf("env file should be restored: %v", err)
	}
}
```

在 `env_test.go` 的 import 中加入 `"strings"`（已有 `"os"`、`"path/filepath"`、`"testing"`）。

- [ ] **Step 2: 跑测试，确认因缺少 `DeleteEnvironment` 失败**

Run: `go test ./internal/workspace -run 'TestDeleteEnvironment' -count=1`

Expected: FAIL，未定义 `DeleteEnvironment`。

- [ ] **Step 3: 实现 `writeActive` 与 `DeleteEnvironment`，并让 `PutLocal` 复用 `writeActive`**

在 `internal/workspace/env.go` 的 `PutLocal` 里，把写 `active.yaml` 的那段换成 `return w.writeActive(local.Environment)`。在 `PutLocal` 之后加入：

```go
func (w *Workspace) writeActive(environment string) error {
	localDir := filepath.Join(w.localDir, "local")
	if err := os.MkdirAll(localDir, 0o700); err != nil {
		return err
	}
	_ = os.Chmod(localDir, 0o700)
	adata, err := yaml.Marshal(&activeFile{Environment: environment})
	if err != nil {
		return err
	}
	return os.WriteFile(filepath.Join(localDir, "active.yaml"), adata, 0o644)
}

func (w *Workspace) DeleteEnvironment(name string) error {
	path, cleaned, err := w.envPath(name)
	if err != nil {
		return err
	}
	local, err := w.GetLocal()
	if err != nil {
		return err
	}
	data, err := os.ReadFile(path)
	if err != nil {
		return err
	}
	if err := os.Remove(path); err != nil {
		return err
	}
	if local.Environment != cleaned {
		return nil
	}
	if err := w.writeActive(""); err != nil {
		_ = os.WriteFile(path, data, 0o644)
		return err
	}
	return nil
}
```

- [ ] **Step 4: 再跑删除相关测试与原有 env 测试**

Run: `go test ./internal/workspace -count=1`

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add internal/workspace/env.go internal/workspace/env_test.go
git commit -m "$(cat <<'EOF'
feat: 删除环境文件并在必要时清空当前环境。

EOF
)"
```

---

### Task 2: workspace 改名环境

**Files:**
- Modify: `internal/workspace/env.go`
- Modify: `internal/workspace/env_test.go`

**Interfaces:**
- Consumes: Task 1 的 `writeActive`、`envPath`、`GetLocal`
- Produces:
  - `var ErrEnvExists = errors.New("environment exists")`
  - `var ErrSameEnvName = errors.New("same environment name")`
  - `func (w *Workspace) RenameEnvironment(oldName, newName string) (Environment, error)`
  - 成功时返回 YAML `name` 已改为新名的 `Environment`；`variables` 来自磁盘旧文件

- [ ] **Step 1: 在 `env_test.go` 末尾追加失败测试**

文件顶部 import 增加 `"errors"`。

```go
func TestRenameEnvironmentMovesFileAndFollowsActive(t *testing.T) {
	ws, err := Init(t.TempDir())
	if err != nil {
		t.Fatal(err)
	}
	if err := ws.PutEnvironment(Environment{Name: "local", Variables: map[string]string{"baseUrl": "http://h"}}); err != nil {
		t.Fatal(err)
	}
	if err := ws.PutLocal(Local{Environment: "local", Secrets: map[string]string{"t": "1"}}); err != nil {
		t.Fatal(err)
	}
	env, err := ws.RenameEnvironment("local", "dev")
	if err != nil {
		t.Fatal(err)
	}
	if env.Name != "dev" || env.Variables["baseUrl"] != "http://h" {
		t.Fatalf("%+v", env)
	}
	if _, err := os.Stat(filepath.Join(ws.Workdir(), "environments", "local.yaml")); !os.IsNotExist(err) {
		t.Fatalf("old file: %v", err)
	}
	got, err := ws.GetLocal()
	if err != nil {
		t.Fatal(err)
	}
	if got.Environment != "dev" || got.Secrets["t"] != "1" {
		t.Fatalf("%+v", got)
	}
}

func TestRenameEnvironmentLeavesOtherActive(t *testing.T) {
	ws, err := Init(t.TempDir())
	if err != nil {
		t.Fatal(err)
	}
	if err := ws.PutEnvironment(Environment{Name: "local", Variables: map[string]string{}}); err != nil {
		t.Fatal(err)
	}
	if err := ws.PutEnvironment(Environment{Name: "prod", Variables: map[string]string{}}); err != nil {
		t.Fatal(err)
	}
	if err := ws.PutLocal(Local{Environment: "prod", Secrets: map[string]string{}}); err != nil {
		t.Fatal(err)
	}
	if _, err := ws.RenameEnvironment("local", "dev"); err != nil {
		t.Fatal(err)
	}
	got, err := ws.GetLocal()
	if err != nil {
		t.Fatal(err)
	}
	if got.Environment != "prod" {
		t.Fatalf("active=%q", got.Environment)
	}
}

func TestRenameEnvironmentTargetExists(t *testing.T) {
	ws, err := Init(t.TempDir())
	if err != nil {
		t.Fatal(err)
	}
	if err := ws.PutEnvironment(Environment{Name: "local", Variables: map[string]string{"k": "1"}}); err != nil {
		t.Fatal(err)
	}
	if err := ws.PutEnvironment(Environment{Name: "prod", Variables: map[string]string{}}); err != nil {
		t.Fatal(err)
	}
	_, err = ws.RenameEnvironment("local", "prod")
	if !errors.Is(err, ErrEnvExists) {
		t.Fatalf("got %v", err)
	}
	if _, err := os.Stat(filepath.Join(ws.Workdir(), "environments", "local.yaml")); err != nil {
		t.Fatalf("old file missing: %v", err)
	}
}

func TestRenameEnvironmentSameName(t *testing.T) {
	ws, err := Init(t.TempDir())
	if err != nil {
		t.Fatal(err)
	}
	if err := ws.PutEnvironment(Environment{Name: "local", Variables: map[string]string{}}); err != nil {
		t.Fatal(err)
	}
	_, err = ws.RenameEnvironment("local", "local")
	if !errors.Is(err, ErrSameEnvName) {
		t.Fatalf("got %v", err)
	}
	if _, err := os.Stat(filepath.Join(ws.Workdir(), "environments", "local.yaml")); err != nil {
		t.Fatal(err)
	}
}

func TestRenameEnvironmentInvalidName(t *testing.T) {
	ws, err := Init(t.TempDir())
	if err != nil {
		t.Fatal(err)
	}
	if err := ws.PutEnvironment(Environment{Name: "local", Variables: map[string]string{}}); err != nil {
		t.Fatal(err)
	}
	_, err = ws.RenameEnvironment("local", "a/b")
	if err == nil || !strings.Contains(err.Error(), "invalid environment name") {
		t.Fatalf("got %v", err)
	}
	if _, err := os.Stat(filepath.Join(ws.Workdir(), "environments", "local.yaml")); err != nil {
		t.Fatal(err)
	}
}

func TestRenameEnvironmentMissing(t *testing.T) {
	ws, err := Init(t.TempDir())
	if err != nil {
		t.Fatal(err)
	}
	_, err = ws.RenameEnvironment("nope", "dev")
	if !os.IsNotExist(err) {
		t.Fatalf("got %v", err)
	}
}

func TestRenameEnvironmentRollsBackWhenActiveWriteFails(t *testing.T) {
	ws, err := Init(t.TempDir())
	if err != nil {
		t.Fatal(err)
	}
	if err := ws.PutEnvironment(Environment{Name: "local", Variables: map[string]string{"k": "v"}}); err != nil {
		t.Fatal(err)
	}
	if err := ws.PutLocal(Local{Environment: "local", Secrets: map[string]string{}}); err != nil {
		t.Fatal(err)
	}
	active := filepath.Join(ws.LocalDir(), "local", "active.yaml")
	if err := os.Remove(active); err != nil {
		t.Fatal(err)
	}
	if err := os.Mkdir(active, 0o755); err != nil {
		t.Fatal(err)
	}
	if _, err := ws.RenameEnvironment("local", "dev"); err == nil {
		t.Fatal("expected error")
	}
	if _, err := os.Stat(filepath.Join(ws.Workdir(), "environments", "local.yaml")); err != nil {
		t.Fatalf("old file should be restored: %v", err)
	}
	if _, err := os.Stat(filepath.Join(ws.Workdir(), "environments", "dev.yaml")); !os.IsNotExist(err) {
		t.Fatal("new file should not remain")
	}
}
```

- [ ] **Step 2: 跑测试，确认因缺少符号失败**

Run: `go test ./internal/workspace -run 'TestRenameEnvironment' -count=1`

Expected: FAIL，未定义 `RenameEnvironment` / `ErrEnvExists` / `ErrSameEnvName`。

- [ ] **Step 3: 实现哨兵错误与 `RenameEnvironment`**

在 `env.go` 的 import 中加入 `"errors"`。在 `type Environment struct` 之前加入：

```go
var (
	ErrEnvExists   = errors.New("environment exists")
	ErrSameEnvName = errors.New("same environment name")
)
```

在 `DeleteEnvironment` 之后加入：

```go
func (w *Workspace) RenameEnvironment(oldName, newName string) (Environment, error) {
	oldPath, oldClean, err := w.envPath(oldName)
	if err != nil {
		return Environment{}, err
	}
	newPath, newClean, err := w.envPath(newName)
	if err != nil {
		return Environment{}, err
	}
	if oldClean == newClean {
		return Environment{}, ErrSameEnvName
	}
	if _, err := os.Stat(newPath); err == nil {
		return Environment{}, ErrEnvExists
	} else if !os.IsNotExist(err) {
		return Environment{}, err
	}
	data, err := os.ReadFile(oldPath)
	if err != nil {
		return Environment{}, err
	}
	var env Environment
	if err := yaml.Unmarshal(data, &env); err != nil {
		return Environment{}, err
	}
	if env.Variables == nil {
		env.Variables = map[string]string{}
	}
	env.Name = newClean
	out, err := yaml.Marshal(&env)
	if err != nil {
		return Environment{}, err
	}
	if err := os.MkdirAll(filepath.Dir(newPath), 0o755); err != nil {
		return Environment{}, err
	}
	if err := os.WriteFile(newPath, out, 0o644); err != nil {
		return Environment{}, err
	}
	if err := os.Remove(oldPath); err != nil {
		_ = os.Remove(newPath)
		return Environment{}, err
	}
	rollback := func() {
		_ = os.WriteFile(oldPath, data, 0o644)
		_ = os.Remove(newPath)
	}
	local, err := w.GetLocal()
	if err != nil {
		rollback()
		return Environment{}, err
	}
	if local.Environment != oldClean {
		return env, nil
	}
	if err := w.writeActive(newClean); err != nil {
		rollback()
		return Environment{}, err
	}
	return env, nil
}
```

- [ ] **Step 4: 再跑 workspace 测试**

Run: `go test ./internal/workspace -count=1`

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add internal/workspace/env.go internal/workspace/env_test.go
git commit -m "$(cat <<'EOF'
feat: 服务端改名环境并同步当前环境。

EOF
)"
```

---

### Task 3: HTTP DELETE 与 rename

**Files:**
- Modify: `internal/server/server.go`
- Modify: `internal/server/handlers.go`
- Modify: `internal/server/handlers_test.go`

**Interfaces:**
- Consumes: `DeleteEnvironment`、`RenameEnvironment`、`ErrEnvExists`、`ErrSameEnvName`
- Produces:
  - `DELETE /api/environments/{name}` → 204
  - `POST /api/environments/{name}/rename` JSON `{ "name": "<新名>" }` → 200 + `Environment`
  - 非法名 / same-name → 400；源不存在 → 404；目标已存在 → 409；`..` 路径段 → 400（现有守卫）

- [ ] **Step 1: 在 `handlers_test.go` 末尾追加失败测试**

```go
func TestEnvironmentDeleteAndRename(t *testing.T) {
	ws, err := workspace.Init(t.TempDir())
	if err != nil {
		t.Fatal(err)
	}
	h := New(ws, nil)
	put := func(name, body string) {
		t.Helper()
		rr := httptest.NewRecorder()
		h.ServeHTTP(rr, apiReq(http.MethodPut, "/api/environments/"+name, []byte(body)))
		if rr.Code != 200 {
			t.Fatalf("put %s %d %s", name, rr.Code, rr.Body.Bytes())
		}
	}
	put("local", `{"name":"local","variables":{"baseUrl":"http://h"}}`)
	put("prod", `{"name":"prod","variables":{}}`)

	rr := httptest.NewRecorder()
	h.ServeHTTP(rr, apiReq(http.MethodPut, "/api/local", []byte(`{"environment":"local","secrets":{"t":"1"}}`)))
	if rr.Code != 200 {
		t.Fatalf("local %d %s", rr.Code, rr.Body.Bytes())
	}

	rr = httptest.NewRecorder()
	h.ServeHTTP(rr, apiReq(http.MethodPost, "/api/environments/local/rename", []byte(`{"name":"dev"}`)))
	if rr.Code != 200 {
		t.Fatalf("rename %d %s", rr.Code, rr.Body.Bytes())
	}
	if !bytes.Contains(rr.Body.Bytes(), []byte(`"name":"dev"`)) {
		t.Fatalf("rename body %s", rr.Body.Bytes())
	}
	rr = httptest.NewRecorder()
	h.ServeHTTP(rr, apiReq(http.MethodGet, "/api/local", nil))
	if rr.Code != 200 || !bytes.Contains(rr.Body.Bytes(), []byte(`"environment":"dev"`)) {
		t.Fatalf("active after rename %s", rr.Body.Bytes())
	}

	rr = httptest.NewRecorder()
	h.ServeHTTP(rr, apiReq(http.MethodPost, "/api/environments/dev/rename", []byte(`{"name":"prod"}`)))
	if rr.Code != 409 {
		t.Fatalf("conflict %d %s", rr.Code, rr.Body.Bytes())
	}

	rr = httptest.NewRecorder()
	h.ServeHTTP(rr, apiReq(http.MethodPost, "/api/environments/dev/rename", []byte(`{"name":"dev"}`)))
	if rr.Code != 400 {
		t.Fatalf("same name %d %s", rr.Code, rr.Body.Bytes())
	}

	rr = httptest.NewRecorder()
	h.ServeHTTP(rr, apiReq(http.MethodPost, "/api/environments/nope/rename", []byte(`{"name":"x"}`)))
	if rr.Code != 404 {
		t.Fatalf("rename missing %d", rr.Code)
	}

	rr = httptest.NewRecorder()
	h.ServeHTTP(rr, apiReq(http.MethodDelete, "/api/environments/prod", nil))
	if rr.Code != 204 {
		t.Fatalf("delete other %d %s", rr.Code, rr.Body.Bytes())
	}
	rr = httptest.NewRecorder()
	h.ServeHTTP(rr, apiReq(http.MethodGet, "/api/local", nil))
	if rr.Code != 200 || !bytes.Contains(rr.Body.Bytes(), []byte(`"environment":"dev"`)) {
		t.Fatalf("active after other delete %s", rr.Body.Bytes())
	}

	rr = httptest.NewRecorder()
	h.ServeHTTP(rr, apiReq(http.MethodDelete, "/api/environments/dev", nil))
	if rr.Code != 204 {
		t.Fatalf("delete current %d", rr.Code)
	}
	rr = httptest.NewRecorder()
	h.ServeHTTP(rr, apiReq(http.MethodGet, "/api/local", nil))
	if rr.Code != 200 || !bytes.Contains(rr.Body.Bytes(), []byte(`"environment":""`)) {
		t.Fatalf("active after delete current %s", rr.Body.Bytes())
	}

	rr = httptest.NewRecorder()
	h.ServeHTTP(rr, apiReq(http.MethodDelete, "/api/environments/dev", nil))
	if rr.Code != 404 {
		t.Fatalf("delete missing %d", rr.Code)
	}

	rr = httptest.NewRecorder()
	h.ServeHTTP(rr, apiReq(http.MethodDelete, "/api/environments/../x", nil))
	if rr.Code != 400 {
		t.Fatalf("escape delete %d", rr.Code)
	}
	rr = httptest.NewRecorder()
	h.ServeHTTP(rr, apiReq(http.MethodPost, "/api/environments/../x/rename", []byte(`{"name":"y"}`)))
	if rr.Code != 400 {
		t.Fatalf("escape rename %d", rr.Code)
	}

	rr = httptest.NewRecorder()
	h.ServeHTTP(rr, apiReq(http.MethodDelete, "/api/environments/a/b", nil))
	if rr.Code != 400 && rr.Code != 404 {
		t.Fatalf("slash name %d", rr.Code)
	}
}
```

`DELETE` 无 body 时 `apiReq` 不设 Content-Type，符合 `guardAPI`（只对 POST/PUT/PATCH 要求 JSON）。

- [ ] **Step 2: 跑测试，确认 404（路由未注册）**

Run: `go test ./internal/server -run TestEnvironmentDeleteAndRename -count=1`

Expected: FAIL，rename/delete 不是 200/204。

- [ ] **Step 3: 注册路由、handler、扩展 `writePathErr`**

`server.go` 在 `PUT /api/environments/{name}` 之后加入：

```go
	mux.HandleFunc("DELETE /api/environments/{name}", s.handleDeleteEnvironment)
	mux.HandleFunc("POST /api/environments/{name}/rename", s.handleRenameEnvironment)
```

`handlers.go` 在 `handlePutEnvironment` 之后加入：

```go
func (s *server) handleDeleteEnvironment(w http.ResponseWriter, r *http.Request) {
	name := r.PathValue("name")
	if err := s.ws.DeleteEnvironment(name); err != nil {
		writePathErr(w, err)
		return
	}
	w.WriteHeader(http.StatusNoContent)
}

func (s *server) handleRenameEnvironment(w http.ResponseWriter, r *http.Request) {
	oldName := r.PathValue("name")
	var body struct {
		Name string `json:"name"`
	}
	if err := json.NewDecoder(r.Body).Decode(&body); err != nil {
		writeJSON(w, http.StatusBadRequest, map[string]string{"error": err.Error()})
		return
	}
	env, err := s.ws.RenameEnvironment(oldName, body.Name)
	if err != nil {
		writePathErr(w, err)
		return
	}
	writeJSON(w, http.StatusOK, env)
}
```

把 `writePathErr` 换成：

```go
func writePathErr(w http.ResponseWriter, err error) {
	if errors.Is(err, workspace.ErrEnvExists) {
		writeJSON(w, http.StatusConflict, map[string]string{"error": err.Error()})
		return
	}
	if errors.Is(err, workspace.ErrSameEnvName) {
		writeJSON(w, http.StatusBadRequest, map[string]string{"error": err.Error()})
		return
	}
	if isInvalidPath(err) {
		writeJSON(w, http.StatusBadRequest, map[string]string{"error": err.Error()})
		return
	}
	if os.IsNotExist(err) || errors.Is(err, os.ErrNotExist) {
		writeJSON(w, http.StatusNotFound, map[string]string{"error": err.Error()})
		return
	}
	writeJSON(w, http.StatusInternalServerError, map[string]string{"error": err.Error()})
}
```

- [ ] **Step 4: 再跑 server 测试**

Run: `go test ./internal/server -count=1`

Expected: PASS。`DELETE /api/environments/a/b` 因 `{name}` 只匹配一段，Go mux 可能 404；测试已允许 400 或 404。

- [ ] **Step 5: Commit**

```bash
git add internal/server/server.go internal/server/handlers.go internal/server/handlers_test.go
git commit -m "$(cat <<'EOF'
feat: 环境删除与改名 API。

EOF
)"
```

---

### Task 4: 前端活动栏类型与环境草稿辅助函数

**Files:**
- Modify: `web/src/activity.ts`
- Modify: `web/src/activity.test.ts`
- Create: `web/src/env.ts`
- Create: `web/src/env.test.ts`

**Interfaces:**
- Consumes: `Environment`、`LocalConfig`（`web/src/types.ts`）
- Produces:
  - `export type LeftView = "collection" | "history" | "environment"`
  - `export interface EnvPair { id: number; key: string; value: string }`
  - `export function varsToPairs(vars: Record<string, string>): EnvPair[]`
  - `export function pairsToVars(pairs: EnvPair[]): Record<string, string>`
  - `export function varsJSON(vars: Record<string, string> | undefined): string`
  - `export function isEnvDirty(pairs: EnvPair[], savedJSON: string): boolean`
  - `export function envNameError(name: string, existing: string[]): string | null`
  - `export function openEnvForEdit(env: Environment): { name: string; pairs: EnvPair[]; snapshot: string }`
  - `export function newEnvPair(key?: string, value?: string): EnvPair`

- [ ] **Step 1: 改 `activity.ts` 的类型，并在 `activity.test.ts` 增加三图标用例**

`activity.ts` 第一行改为：

```ts
export type LeftView = "collection" | "history" | "environment";
```

`clickActivityIcon` / `togglePanel` 函数体不改。

在 `activity.test.ts` 的 `clickActivityIcon` describe 末尾追加：

```ts
  it("switches from collection to environment and opens", () => {
    expect(clickActivityIcon(openCollection, "environment")).toEqual({
      view: "environment",
      panelOpen: true,
    });
  });

  it("collapses environment when clicking the active icon", () => {
    expect(
      clickActivityIcon({ view: "environment", panelOpen: true }, "environment"),
    ).toEqual({ view: "environment", panelOpen: false });
  });
```

在 `togglePanel` describe 追加：

```ts
  it("toggles environment panel without changing view", () => {
    expect(
      togglePanel({ view: "environment", panelOpen: true }),
    ).toEqual({ view: "environment", panelOpen: false });
  });
```

- [ ] **Step 2: 写 `web/src/env.test.ts`（此时 `env.ts` 还不存在）**

```ts
import { describe, expect, it } from "vitest";
import {
  envNameError,
  isEnvDirty,
  openEnvForEdit,
  pairsToVars,
  varsJSON,
  varsToPairs,
} from "./env";
import type { LocalConfig } from "./types";

describe("pairs", () => {
  it("drops empty keys and lets later duplicate win", () => {
    const vars = pairsToVars([
      { id: 1, key: "a", value: "1" },
      { id: 2, key: "", value: "x" },
      { id: 3, key: "a", value: "2" },
    ]);
    expect(vars).toEqual({ a: "2" });
  });

  it("uses one blank row when empty", () => {
    const pairs = varsToPairs({});
    expect(pairs).toHaveLength(1);
    expect(pairs[0].key).toBe("");
    expect(pairs[0].value).toBe("");
  });
});

describe("isEnvDirty", () => {
  it("is dirty after changing a value and clean after matching snapshot", () => {
    const env = { name: "local", variables: { baseUrl: "http://h" } };
    const opened = openEnvForEdit(env);
    expect(isEnvDirty(opened.pairs, opened.snapshot)).toBe(false);
    const dirtyPairs = opened.pairs.map((p) =>
      p.key === "baseUrl" ? { ...p, value: "http://other" } : p,
    );
    expect(isEnvDirty(dirtyPairs, opened.snapshot)).toBe(true);
    const saved = varsJSON(pairsToVars(dirtyPairs));
    expect(isEnvDirty(dirtyPairs, saved)).toBe(false);
  });
});

describe("envNameError", () => {
  it("rejects empty, slash, and existing names", () => {
    expect(envNameError("", ["local"])).not.toBeNull();
    expect(envNameError("a/b", [])).not.toBeNull();
    expect(envNameError("local", ["local"])).not.toBeNull();
    expect(envNameError("dev", ["local"])).toBeNull();
  });
});

describe("openEnvForEdit", () => {
  it("does not change local.environment", () => {
    const local: LocalConfig = { environment: "local", secrets: {} };
    const draft = openEnvForEdit({ name: "prod", variables: { a: "1" } });
    expect(draft.name).toBe("prod");
    expect(pairsToVars(draft.pairs)).toEqual({ a: "1" });
    expect(local.environment).toBe("local");
  });
});
```

- [ ] **Step 3: 跑前端测试，确认失败**

Run: `cd web && npm test`

Expected: FAIL，`./env` 无法解析；activity 新用例应 PASS。

- [ ] **Step 4: 实现 `web/src/env.ts`**

```ts
import type { Environment } from "./types";

export interface EnvPair {
  id: number;
  key: string;
  value: string;
}

let pairSeq = 0;
function nextPairId(): number {
  pairSeq += 1;
  return pairSeq;
}

export function newEnvPair(key = "", value = ""): EnvPair {
  return { id: nextPairId(), key, value };
}

export function varsToPairs(vars: Record<string, string>): EnvPair[] {
  const rows = Object.entries(vars).map(([key, value]) =>
    newEnvPair(key, value),
  );
  return rows.length > 0 ? rows : [newEnvPair()];
}

export function pairsToVars(pairs: EnvPair[]): Record<string, string> {
  const out: Record<string, string> = {};
  for (const p of pairs) {
    if (p.key) out[p.key] = p.value;
  }
  return out;
}

export function varsJSON(vars: Record<string, string> | undefined): string {
  const src = vars ?? {};
  const keys = Object.keys(src).sort();
  const ordered: Record<string, string> = {};
  for (const k of keys) {
    ordered[k] = src[k];
  }
  return JSON.stringify(ordered);
}

export function isEnvDirty(pairs: EnvPair[], savedJSON: string): boolean {
  return varsJSON(pairsToVars(pairs)) !== savedJSON;
}

export function envNameError(name: string, existing: string[]): string | null {
  const trimmed = name.trim();
  if (!trimmed) return "名称不能为空";
  if (trimmed.includes("/") || trimmed.includes("\\")) {
    return "名称不能含 /";
  }
  if (trimmed.startsWith("..") || trimmed === "." || trimmed === "..") {
    return "名称非法";
  }
  if (existing.includes(trimmed)) return "已有同名环境";
  return null;
}

export function openEnvForEdit(env: Environment): {
  name: string;
  pairs: EnvPair[];
  snapshot: string;
} {
  const variables = env.variables ?? {};
  return {
    name: env.name,
    pairs: varsToPairs(variables),
    snapshot: varsJSON(variables),
  };
}
```

- [ ] **Step 5: 再跑前端测试**

Run: `cd web && npm test`

Expected: PASS。

- [ ] **Step 6: Commit**

```bash
git add web/src/activity.ts web/src/activity.test.ts web/src/env.ts web/src/env.test.ts
git commit -m "$(cat <<'EOF'
feat: 环境视图类型与变量草稿辅助函数。

EOF
)"
```

---

### Task 5: API 客户端与对话框扩展

**Files:**
- Modify: `web/src/api.ts`
- Modify: `web/src/Dialog.tsx`

**Interfaces:**
- Consumes: Task 3 的 HTTP 契约；现有 `parseJSON`、`Environment`
- Produces:
  - `export async function deleteEnvironment(name: string): Promise<void>`
  - `export async function renameEnvironment(name: string, next: string): Promise<Environment>`
  - `DialogMode` path 增加可选 `hint?: string`；`intent` 增加 `"create-env" | "rename-env"`；confirm 增加可选 `subject?: "request" | "environment"`（缺省当 `"request"`，现有删除请求不用改也能编过）

- [ ] **Step 1: 在 `api.ts` 的 `putEnvironment` 之后加入**

```ts
export async function deleteEnvironment(name: string): Promise<void> {
  const res = await fetch(`/api/environments/${encodeURIComponent(name)}`, {
    method: "DELETE",
  });
  await parseJSON<void>(res);
}

export async function renameEnvironment(
  name: string,
  next: string,
): Promise<Environment> {
  const res = await fetch(
    `/api/environments/${encodeURIComponent(name)}/rename`,
    {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ name: next }),
    },
  );
  return parseJSON<Environment>(res);
}
```

- [ ] **Step 2: 扩展 `Dialog.tsx` 的类型与 hint 文案**

把 `DialogMode` 换成：

```ts
export type DialogMode =
  | {
      kind: "path";
      title: string;
      submitLabel: string;
      error: string | null;
      intent: "create" | "save" | "create-env" | "rename-env";
      hint?: string;
    }
  | {
      kind: "confirm";
      title: string;
      body: string;
      submitLabel: string;
      error: string | null;
      path: string;
      subject?: "request" | "environment";
    };
```

把硬编码的 `<p>相对 collections/，例如 auth/ping</p>` 换成：

```tsx
            <p>
              {mode.hint ?? "相对 collections/，例如 auth/ping"}
            </p>
```

请求对话框不传 `hint`，文案与现在相同。

- [ ] **Step 3: 确认前端类型检查与测试仍过**

Run: `cd web && npx tsc --noEmit && npm test`

Expected: PASS。`App.tsx` 仍只使用 `"create" | "save"`，可赋值给加宽后的 union。

- [ ] **Step 4: Commit**

```bash
git add web/src/api.ts web/src/Dialog.tsx
git commit -m "$(cat <<'EOF'
feat: 环境删除/改名客户端与对话框 hint。

EOF
)"
```

---

### Task 6: EnvEditor、活动栏图标、侧栏环境列表

**Files:**
- Create: `web/src/EnvEditor.tsx`
- Modify: `web/src/ActivityBar.tsx`
- Modify: `web/src/Sidebar.tsx`
- Modify: `web/src/styles.css`
- Modify: `web/src/App.tsx`（本任务只改 Sidebar/ActivityBar 的 props，使工程能编译：先把新 props 接上，主栏替换放到 Task 7。若本任务结束时 App 未接环境 CRUD，也必须能 `tsc --noEmit`。）

为避免 App 半成品无法编译，**本任务在改 Sidebar/ActivityBar 的同时改 App**：ActivityBar 已能点第三项；Sidebar 在 `view === "environment"` 时渲染列表；主栏仍是请求编辑器（Task 7 再换成 EnvEditor）。点环境树只 `setEditingEnv`，不调用 `putLocal`。

**Interfaces:**
- Consumes: Task 4 的 `LeftView`、`openEnvForEdit`；Task 5 的 Dialog intent（本任务 App 先接新建/删除对话框，Save/改名在 Task 7）
- Produces: `EnvEditor` 组件（本任务创建文件，Task 7 才挂到主栏）；Sidebar 环境视图

本任务范围收紧为：**ActivityBar 第三图标 + Sidebar 环境列表 + EnvEditor 文件存在且可编译**。App 接上树选中状态 `editingEnv`，打开新建/删除对话框但提交逻辑可在本任务用 `putEnvironment` / `deleteEnvironment` 做完（与规格新建/删除一致），主栏仍显示请求区。这样本任务可手动点开环境树验证列表。

为保持「每个任务可测」，本任务 App 必须：

- 第三图标可切换 `left.view`
- 环境树点选只改 `editingEnv`，不 `putLocal`
- 新建 / 删除走对话框并调 API、刷新 `envs` 与 `local`

主栏 EnvEditor 与 `Ctrl+S` 环境保存放到 Task 7，避免 App.tsx 单任务过大。

- [ ] **Step 1: 创建 `web/src/EnvEditor.tsx`**

```tsx
import { newEnvPair, type EnvPair } from "./env";

interface Props {
  name: string | null;
  pairs: EnvPair[];
  dirty: boolean;
  saving: boolean;
  error: string | null;
  onPairsChange: (next: EnvPair[]) => void;
  onSave: () => void;
  onRename: () => void;
}

export default function EnvEditor({
  name,
  pairs,
  dirty,
  saving,
  error,
  onPairsChange,
  onSave,
  onRename,
}: Props) {
  if (!name) {
    return (
      <div className="env-editor">
        <div className="empty-state">
          <p className="empty-title">在左侧选择一个环境，或新建</p>
        </div>
      </div>
    );
  }
  return (
    <div className="env-editor">
      <div className="urlbar">
        <span className="env-editor-name" title={name}>
          {name}
        </span>
        {dirty && <span className="dirty">未保存</span>}
        <button type="button" className="btn" onClick={onRename}>
          改名
        </button>
        <button
          type="button"
          className="btn"
          onClick={onSave}
          disabled={saving || !dirty}
        >
          Save
        </button>
      </div>
      {error ? <p className="error env-editor-error">{error}</p> : null}
      <div className="env-editor-body">
        <table className="kv-table">
          <thead>
            <tr>
              <th>键</th>
              <th>值</th>
              <th />
            </tr>
          </thead>
          <tbody>
            {pairs.map((p, i) => (
              <tr key={p.id}>
                <td>
                  <input
                    value={p.key}
                    onChange={(e) =>
                      onPairsChange(
                        pairs.map((row, j) =>
                          j === i ? { ...row, key: e.target.value } : row,
                        ),
                      )
                    }
                  />
                </td>
                <td>
                  <input
                    type="text"
                    value={p.value}
                    onChange={(e) =>
                      onPairsChange(
                        pairs.map((row, j) =>
                          j === i ? { ...row, value: e.target.value } : row,
                        ),
                      )
                    }
                  />
                </td>
                <td>
                  <button
                    type="button"
                    className="btn-icon"
                    onClick={() => {
                      const next = pairs.filter((_, j) => j !== i);
                      onPairsChange(next.length ? next : [newEnvPair()]);
                    }}
                  >
                    ×
                  </button>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
        <button
          type="button"
          className="btn"
          onClick={() => onPairsChange([...pairs, newEnvPair()])}
        >
          添加变量
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: `ActivityBar.tsx` 在历史按钮之后加入环境图标**

```tsx
      <button
        type="button"
        className={
          view === "environment" && panelOpen
            ? "activity-btn active"
            : "activity-btn"
        }
        title="环境"
        aria-label="环境"
        aria-pressed={view === "environment" && panelOpen}
        onClick={() => onClickIcon("environment")}
      >
        <svg viewBox="0 0 24 24" aria-hidden="true">
          <path d="M4.8 8.2h14.4v11.3H4.8z" />
          <path d="M8.2 8.2V5.8h7.6v2.4" />
          <path d="M4.8 12.8h14.4" />
        </svg>
      </button>
```

- [ ] **Step 3: 扩展 `Sidebar.tsx`**

在 `import type { LeftView } from "./activity";` 旁增加 `Environment`。Props 增加：

```ts
  envs: Environment[];
  editingEnv: string | null;
  activeEnv: string;
  onSelectEnv: (name: string) => void;
  onNewEnv: () => void;
  onDeleteEnv: (name: string) => void;
```

在 `if (view === "collection")` 块之后、历史 return 之前插入：

```tsx
  if (view === "environment") {
    return (
      <aside className="sidebar">
        <div className="sidebar-head">
          环境
          <button
            type="button"
            className="btn-icon"
            title="新建"
            onClick={onNewEnv}
          >
            ＋
          </button>
        </div>
        <div className="sidebar-body">
          {envs.length === 0 ? (
            <div className="empty-state">
              <p className="empty-title">还没有环境</p>
              <p className="empty-hint">用标题栏 ＋ 或下方按钮创建</p>
              <button type="button" className="btn" onClick={onNewEnv}>
                新建环境
              </button>
            </div>
          ) : (
            <ul className="req-list">
              {envs.map((env) => (
                <li key={env.name}>
                  <div className="tree-row">
                    <button
                      type="button"
                      className={
                        editingEnv === env.name ? "req-item active" : "req-item"
                      }
                      onClick={() => onSelectEnv(env.name)}
                    >
                      <span className="req-path">{env.name}</span>
                      {activeEnv === env.name ? (
                        <span className="env-send-badge">发送</span>
                      ) : null}
                    </button>
                    <button
                      type="button"
                      className="btn-icon"
                      title="删除"
                      onClick={(e) => {
                        e.stopPropagation();
                        onDeleteEnv(env.name);
                      }}
                    >
                      ×
                    </button>
                  </div>
                </li>
              ))}
            </ul>
          )}
        </div>
      </aside>
    );
  }
```

- [ ] **Step 4: `styles.css` 追加**

```css
.env-send-badge {
  color: var(--muted);
  font-size: 12px;
  margin-left: 0.35rem;
}

.env-editor {
  flex: 1;
  min-height: 0;
  display: flex;
  flex-direction: column;
}

.env-editor-name {
  font-family: var(--mono);
  flex: 1;
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.env-editor-body {
  padding: 0.6rem;
  overflow: auto;
}

.env-editor-error {
  margin: 0.4rem 0.6rem 0;
}
```

- [ ] **Step 5: 改 `App.tsx` 使 Sidebar 能编译，并接上环境树 / 新建 / 删除（主栏仍是请求）**

Import 增加：`putEnvironment`、`deleteEnvironment`、`envNameError`。

状态增加：

```ts
  const [editingEnv, setEditingEnv] = useState<string | null>(null);
```

`onSelectEnv`：

```ts
  function onSelectEnv(name: string) {
    setEditingEnv(name);
  }
```

本任务 `openEnvForEdit` 的返回值还不用进 state（Task 7 才用）。**不要**在 `onSelectEnv` 里调用 `putLocal`。

新建：

```ts
  function openNewEnvDialog() {
    setDialogError(null);
    setDialog({
      kind: "path",
      title: "环境名",
      submitLabel: "创建",
      error: null,
      intent: "create-env",
      hint: "文件名，例如 local，不能含 /",
    });
  }
```

删除：

```ts
  function openDeleteEnvDialog(name: string) {
    const isActive = local?.environment === name;
    setDialogError(null);
    setDialog({
      kind: "confirm",
      title: "删除环境",
      body: isActive
        ? `删除 ${name}？此操作会从磁盘去掉该文件。顶栏当前环境将变为未选择。`
        : `删除 ${name}？此操作会从磁盘去掉该文件。`,
      submitLabel: "删除",
      error: null,
      path: name,
      subject: "environment",
    });
  }
```

扩展 `onDialogSubmit`：在 `current.kind === "path"` 分支里，若 `intent === "create-env"`：

```ts
        if (current.intent === "create-env") {
          const err = envNameError(p, envs.map((e) => e.name));
          if (err) {
            setDialogError(err);
            return;
          }
          await putEnvironment(p, { name: p, variables: {} });
          const list = await getEnvironments();
          setEnvs(list);
          setEditingEnv(p);
          setDialog(null);
          setDialogError(null);
          return;
        }
```

`kind === "confirm"` 时：

```ts
        if (current.subject === "environment") {
          await deleteEnvironment(current.path);
          const [list, loc] = await Promise.all([getEnvironments(), getLocal()]);
          setEnvs(list);
          setLocal(loc);
          if (editingEnv === current.path) setEditingEnv(null);
          setDialog(null);
          setDialogError(null);
          return;
        }
        await deleteRequest(current.path);
        // ...现有请求删除逻辑
```

`Sidebar` JSX 传入：

```tsx
            envs={envs}
            editingEnv={editingEnv}
            activeEnv={local?.environment ?? ""}
            onSelectEnv={onSelectEnv}
            onNewEnv={openNewEnvDialog}
            onDeleteEnv={openDeleteEnvDialog}
```

`onDialogSubmit` 里 `editingEnv` 请用 `editingEnvRef`（与 `currentPathRef` 同样模式），避免闭包过期。

- [ ] **Step 6: 类型检查与测试**

Run: `cd web && npx tsc --noEmit && npm test`

Expected: PASS。

- [ ] **Step 7: Commit**

```bash
git add web/src/EnvEditor.tsx web/src/ActivityBar.tsx web/src/Sidebar.tsx web/src/styles.css web/src/App.tsx
git commit -m "$(cat <<'EOF'
feat: 活动栏环境项与环境树。

EOF
)"
```

---

### Task 7: 主栏 EnvEditor、保存/改名、快捷键、README

**Files:**
- Modify: `web/src/App.tsx`
- Modify: `README.md`
- Modify: `internal/server/ui/`（`cd web && npm run build` 的嵌入产物）

**Interfaces:**
- Consumes: `EnvEditor`、`renameEnvironment`、`putEnvironment`、`isEnvDirty`、`openEnvForEdit`、`pairsToVars`
- Produces: 环境视图下主栏为 EnvEditor；`Ctrl+S` 保存环境；`Ctrl+Enter` 不发送；改名走对话框并 `GET /api/local`

- [ ] **Step 1: 在 App 增加环境草稿 state**

```ts
  const [envPairs, setEnvPairs] = useState<EnvPair[]>(() => varsToPairs({}));
  const [envDirty, setEnvDirty] = useState(false);
  const [envSaving, setEnvSaving] = useState(false);
  const [envError, setEnvError] = useState<string | null>(null);
  const envSavedRef = useRef(varsJSON({}));
  const envPairsRef = useRef(envPairs);
  envPairsRef.current = envPairs;
  const editingEnvRef = useRef(editingEnv);
  editingEnvRef.current = editingEnv;
  const leftRef = useRef(left);
  leftRef.current = left;
  const envsRef = useRef(envs);
  envsRef.current = envs;
```

需要的 import：`EnvPair`、`isEnvDirty`、`openEnvForEdit`、`pairsToVars`、`varsJSON`、`varsToPairs`、`renameEnvironment`、`EnvEditor`。

加载编辑：

```ts
  function loadEnv(env: Environment) {
    const opened = openEnvForEdit(env);
    setEditingEnv(opened.name);
    setEnvPairs(opened.pairs);
    envSavedRef.current = opened.snapshot;
    setEnvDirty(false);
    setEnvError(null);
  }

  function onSelectEnv(name: string) {
    const env = envs.find((e) => e.name === name);
    if (!env) return;
    loadEnv(env);
  }

  function onEnvPairsChange(next: EnvPair[]) {
    setEnvPairs(next);
    setEnvDirty(isEnvDirty(next, envSavedRef.current));
  }
```

保存：

```ts
  const onSaveEnv = useCallback(async () => {
    const name = editingEnvRef.current;
    if (!name) return;
    if (!isEnvDirty(envPairsRef.current, envSavedRef.current)) return;
    setEnvSaving(true);
    setEnvError(null);
    try {
      const saved = await putEnvironment(name, {
        name,
        variables: pairsToVars(envPairsRef.current),
      });
      const list = await getEnvironments();
      setEnvs(list);
      loadEnv(saved);
    } catch (err) {
      setEnvError(err instanceof Error ? err.message : String(err));
    } finally {
      setEnvSaving(false);
    }
  }, []);
```

注意：`loadEnv` 若不用 `useCallback`，不要放进 `onSaveEnv` 依赖；改为在 `onSaveEnv` 内联同样的 `openEnvForEdit(saved)` 赋值，或把 `loadEnv` 也做成 `useCallback`。推荐 `loadEnv` 用 `useCallback`，`onSaveEnv` 依赖它。

改名对话框：

```ts
  function openRenameEnvDialog() {
    const name = editingEnvRef.current;
    if (!name) return;
    const isActive = local?.environment === name;
    setDialogError(null);
    setDialog({
      kind: "path",
      title: "重命名环境",
      submitLabel: "重命名",
      error: null,
      intent: "rename-env",
      hint: isActive
        ? "文件名，例如 local，不能含 /。顶栏当前环境将改为新名称。"
        : "文件名，例如 local，不能含 /",
    });
  }
```

`onDialogSubmit` 增加 `intent === "rename-env"`：

```ts
        if (current.intent === "rename-env") {
          const oldName = editingEnvRef.current;
          if (!oldName) return;
          const others = envsRef.current
            .map((e) => e.name)
            .filter((n) => n !== oldName);
          const err = envNameError(p, others);
          if (err) {
            setDialogError(err);
            return;
          }
          const renamed = await renameEnvironment(oldName, p);
          const [list, loc] = await Promise.all([
            getEnvironments(),
            getLocal(),
          ]);
          setEnvs(list);
          setLocal(loc);
          loadEnv(renamed);
          setDialog(null);
          setDialogError(null);
          return;
        }
```

新建成功后改为 `loadEnv({ name: p, variables: {} })`，不要只 `setEditingEnv`。

删除正在编辑的项后：`setEditingEnv(null)` 并 `setEnvPairs(varsToPairs({}))`、`setEnvDirty(false)`。

快捷键 `onKey`：

```ts
      if (action === "send") {
        e.preventDefault();
        if (leftRef.current.view === "environment") return;
        if (!sendingRef.current) void onSend();
      } else if (action === "save") {
        if (leftRef.current.view === "environment") {
          void onSaveEnv();
        } else {
          void onSave();
        }
      }
```

`useEffect` 依赖数组加入 `onSaveEnv`。

主栏 JSX：`left.view === "environment"` 时渲染：

```tsx
        <div className="work">
          {left.view === "environment" ? (
            <EnvEditor
              name={editingEnv}
              pairs={envPairs}
              dirty={envDirty}
              saving={envSaving}
              error={envError}
              onPairsChange={onEnvPairsChange}
              onSave={() => void onSaveEnv()}
              onRename={openRenameEnvDialog}
            />
          ) : (
            <>
              <UrlBar
                draft={draft}
                dirty={dirty}
                sending={sending}
                saving={saving}
                onChange={(next) => applyDraft(next)}
                onSend={() => void onSend()}
                onStop={() => void onStop()}
                onSave={() => void onSave()}
              />
              <div className="panes">
                <RequestEditor
                  draft={draft}
                  onChange={(next) => applyDraft(next)}
                />
                <ErrorBoundary>
                  <ResponsePane result={result} />
                </ErrorBoundary>
              </div>
            </>
          )}
        </div>
```

请求草稿 state 在切到环境视图时不要清掉。

- [ ] **Step 2: 更新 README「界面」一节**

把：

```
左侧活动栏切换集合与历史；再点当前图标收起树（`Ctrl+B`）。树展开时宽 220px。Method、URL、Send、Stop、Save 横跨中间请求栏和右侧响应栏（`Ctrl+Enter` 发送，`Ctrl+S` 保存）。
```

换成：

```
左侧活动栏切换集合、历史与环境；再点当前图标收起树（`Ctrl+B`）。树展开时宽 220px。集合/历史视图下 Method、URL、Send、Stop、Save 横跨中间请求栏和右侧响应栏（`Ctrl+Enter` 发送，`Ctrl+S` 保存请求）。环境视图左侧列环境名，中间编辑模板变量（`Ctrl+S` 保存环境，`Ctrl+Enter` 不发送）；树选中只打开编辑，顶栏环境选择器才是发送用的当前环境。新建/改名/删除在应用内对话框完成，不使用浏览器原生弹窗。
```

删掉后文重复的「新建/删除在应用内对话框…」若已并入上段。

- [ ] **Step 3: 类型检查、前端测试、Go 测试、嵌入构建**

Run:

```
cd web && npx tsc --noEmit && npm test && npm run build
go test ./...
```

Expected: 全部 PASS；`internal/server/ui/` 出现新的 hashed 资源（旧 hash 文件若不再被引用应随 Vite 构建被替换，把构建产生的变更一并提交）。

- [ ] **Step 4: Commit**

```bash
git add web/src/App.tsx README.md internal/server/ui
git commit -m "$(cat <<'EOF'
feat: 环境编辑器、保存改名与快捷键。

EOF
)"
```

---

## Self-review（对照规格）

| 规格项 | 任务 |
|--------|------|
| 活动栏第三项、树选中 ≠ 顶栏 | Task 4、6、7 |
| 显式 Save、未保存、`Ctrl+S` | Task 4 `isEnvDirty`、Task 7 |
| 新建 / 改名 / 删除 + 确认对话框 | Task 5–7 |
| 删/改当前环境写 `active.yaml` | Task 1–3 |
| 密钥面板不改 | 无任务改 `TopBar` 密钥 |
| `PUT` upsert；UI 拦同名新建 | Task 3 不改 PUT；Task 6 `envNameError` |
| 409 不覆盖改名目标 | Task 2–3 |
| 回滚 active 写入失败 | Task 1–2 |
| `Ctrl+Enter` 环境视图无操作 | Task 7 |
| 坏 YAML / 缺目录仍旧行为 | 不改 `ListEnvironments` |
| README 界面 | Task 7 |
| 前端脏标记与树选中不改 local | Task 4 |
| 嵌入构建 | Task 7 |

无 TBD。函数名前后一致：`DeleteEnvironment`、`RenameEnvironment`、`ErrEnvExists`、`ErrSameEnvName`、`openEnvForEdit`、`envNameError`。
