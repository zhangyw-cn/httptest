# httptest README Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在仓库根目录新增一份简体中文 `README.md`，覆盖启动、CLI、三栏 UI、工作区、请求/环境示例、v1 边界与开发命令。

**Architecture:** 只新增根目录一个 Markdown 文件。内容按 `docs/superpowers/specs/2026-09-03-readme-design.md` 的固定目录一次写完；事实以 `cmd/httptest/main.go`、`web/vite.config.ts` 和 `internal/workspace` 的 YAML 类型为准。不改产品代码、不新增测试包、不新增其它文档。

**Tech Stack:** Markdown；构建命令为 Go 1.22+ 的 `go build ./cmd/httptest`。

## Global Constraints

- 仅创建 `README.md`；语言简体中文；无截图、无徽章、无 License、无 `CONTRIBUTING.md`。
- 章节顺序固定：标题 → 能做什么 → 快速开始 → 命令行 → 界面 → 工作区 → 请求文件 → 环境与密钥 → v1 明确不做 → 开发 → 相关文档。不增节、不调序。
- 不要 `git clone` URL、不要 `go install` 公开模块地址、命令块不要 `cd`。
- CLI 只写 `--listen` 与 `--open`；默认 `127.0.0.1:1370`。
- 不写 HTTP API 表、错误分类表、2MiB 截断、User-Agent、CSRF/Host 校验。
- 若文档与代码冲突，改 README，不改产品行为。
- 规格写明无自动化 README 测试；验收用本计划中的 `go build` 与 `grep`。

## File Structure

| 路径 | 职责 |
|------|------|
| `README.md` | 仓库唯一面向使用者/贡献者的入口文档 |

不修改：`cmd/`、`internal/`、`web/`、`docs/superpowers/specs/2026-09-01-httptest-design.md`。

---

### Task 1: 写入 README.md

**Files:**
- Create: `README.md`

**Interfaces:**
- Consumes: `docs/superpowers/specs/2026-09-03-readme-design.md` 各节正文；`go.mod` 的 `go 1.22`；`cmd/httptest/main.go` 的 `--listen` / `--open`；`web/vite.config.ts` 的 `outDir: "../internal/server/ui"`。
- Produces: 根目录 `README.md`，含下列 11 个标题（`# httptest` 与十个 `##`）。

- [ ] **Step 1: 创建 `README.md`，内容必须与下面全文一致（可微调换行，不可增删节或改命令/字段名）**

外层用四个反引号，避免内嵌 ` ```bash ` / ` ```yaml ` 提前结束围栏。写入文件时从 `# httptest` 起，到相关文档那句止，不要包含外层围栏。

````markdown
# httptest

本机 HTTP 调试工具。在项目目录运行一个 Go 二进制，浏览器打开 Web UI，由 Go 后端发出请求（避开浏览器 CORS），并展示请求、响应与诊断。

## 能做什么

- 一条命令启动；默认监听 `127.0.0.1:1370`，仅本机可访问
- 编辑并发送 GET、POST、PUT、PATCH、DELETE、HEAD、OPTIONS；未保存草稿也能发
- 响应区：状态码、头、格式化 Body、原始报文、体积、重定向链、分阶段耗时
- 集合与环境模板可随项目 git 共享；历史、密钥、本机覆盖不进 git
- 数据全部落在启动时当前目录的 `.httptest/`

## 快速开始

需要 Go 1.22+。克隆本仓库后，在仓库根目录执行：

```bash
go build -o httptest ./cmd/httptest
./httptest --open
```

`net.Listen` 成功之后才会在 stdout 打印实际 URL（例如 `http://127.0.0.1:1370`）。`--open` 会尽力打开系统浏览器；失败则忽略，请手动打开打印出的地址。

浏览器打开后：选环境 → 填 Method/URL → Send。

## 命令行

```
httptest [--listen 127.0.0.1:1370] [--open]
```

- 工作区固定为当前工作目录下的 `.httptest/`，没有 `--workspace`。
- `--listen` 非法则退出码非 0。
- 监听 `0.0.0.0`、`::` 或空 host 时，stderr 会警告：能连到该端口的人可把本机当 HTTP 代理。默认请继续用回环地址。

## 界面

顶栏是产品名、工作区路径（cwd）和环境选择器。左栏为 `集合 | 历史`：集合是可新建、删除请求的目录树；历史按时间倒序，点击后回填中栏并展示当时响应。中栏是 Method、URL 和 `Query | Headers | Body`，底部为 **Send**、**Stop**、**Save**；Send 发送当前草稿。右栏展示状态码、总耗时、体积，以及 `Body | Headers | Raw | Timeline`。

## 工作区

首次启动若目录不存在会创建，并写入 `.httptest/.gitignore`：

```
local/
history/
```

```
.httptest/
  .gitignore
  collections/      # 请求，可提交
  environments/     # 环境模板，可提交
  local/            # 密钥与当前环境，不提交
  history/          # 按日 jsonl，不提交
```

请求身份是相对 `collections/` 的路径、不含 `.yaml` 后缀（例如 `auth/login`）。重命名文件即改名，移动目录即改分组，不另建 ID。

## 请求文件

路径：`collections/**/*.yaml`。

```yaml
name: Login
method: POST
url: "{{baseUrl}}/api/login"
query: {}
headers:
  Accept: application/json
body:
  type: json    # none | json | raw | form
  content: |
    {"user": "{{username}}"}
timeout: 30s    # 可选，默认 30s
```

- `method` 仅允许 GET、POST、PUT、PATCH、DELETE、HEAD、OPTIONS（大小写不敏感，落盘大写）。
- `query`、`headers`：string → string，无同名多值。
- `body.type` 为 `form` 时，`content` 为 YAML map，发送 `application/x-www-form-urlencoded`。
- `body.type` 为 `json` 且未设 `Content-Type` 时，默认 `application/json`。
- `body.type` 为 `none` 或 GET/HEAD 时不发送 body。
- URL、query、header、body 文本均可出现 `{{name}}`。

## 环境与密钥

`environments/<name>.yaml` 可提交，禁止放密钥：

```yaml
name: local
variables:
  baseUrl: http://127.0.0.1:8080
  username: demo
```

`local/secrets.yaml` 用同名变量覆盖模板，不提交。`local/active.yaml` 记录当前环境：

```yaml
environment: local
```

替换顺序：环境模板 → `secrets.yaml`。缺失任一 `{{var}}` 时分类为 `invalid`，列出变量名，**不发送**。密钥可在 UI 中编辑（本机工具，不上传）。

## v1 明确不做

Cookie 管理、Basic/Bearer 助手、multipart 上传、HTTP/2、WebSocket、gRPC、SSE、多请求标签页、账号登录、云同步、跳过 TLS 校验、关闭跟随重定向。

跟随重定向最多 10 次。HTTPS 使用系统 CA。出站忽略 `HTTP_PROXY` / `HTTPS_PROXY`。

## 开发

```bash
go test ./...
cd web && npm test
```

改前端后重建嵌入资源（Vite 输出到 `internal/server/ui/`，由 Go `embed`，无需手工复制）：

```bash
cd web && npm run build
go build -o httptest ./cmd/httptest
```

开发时可用 `web/` 的 Vite（`npm run dev`）把 `/api` 代理到 `http://127.0.0.1:1370`，须同时先启动 Go 服务。产品形态仍是单二进制。

```
cmd/httptest/
internal/workspace/
internal/executor/
internal/server/
web/
```

## 相关文档

实现与文件格式细节见 [docs/superpowers/specs/2026-09-01-httptest-design.md](docs/superpowers/specs/2026-09-01-httptest-design.md)。
````

- [ ] **Step 2: 核对标题顺序与必现字符串**

Run:

```bash
python3 - <<'PY'
from pathlib import Path
text = Path("README.md").read_text(encoding="utf-8")
headings = [ln.strip() for ln in text.splitlines() if ln.startswith("#")]
expected = [
    "# httptest",
    "## 能做什么",
    "## 快速开始",
    "## 命令行",
    "## 界面",
    "## 工作区",
    "## 请求文件",
    "## 环境与密钥",
    "## v1 明确不做",
    "## 开发",
    "## 相关文档",
]
assert headings == expected, headings
needles = [
    "Go 1.22+",
    "go build -o httptest ./cmd/httptest",
    "./httptest --open",
    "127.0.0.1:1370",
    "[--listen 127.0.0.1:1370] [--open]",
    "没有 `--workspace`",
    "collections/**/*.yaml",
    "{{baseUrl}}",
    "{{username}}",
    "secrets.yaml",
    "invalid",
    "go test ./...",
    "npm run build",
    "internal/server/ui/",
    "docs/superpowers/specs/2026-09-01-httptest-design.md",
    "HTTP_PROXY",
]
missing = [n for n in needles if n not in text]
assert not missing, missing
print("headings and needles OK")
print("lines", len(text.splitlines()))
PY
```

Expected: stdout 含 `headings and needles OK`；`lines` 大约在 150–250（略少亦可，不可超过约 280）。

- [ ] **Step 3: 确认禁止出现的内容**

Run:

```bash
python3 - <<'PY'
from pathlib import Path
text = Path("README.md").read_text(encoding="utf-8")
forbidden = [
    "go install",
    "github.com/",
    "gitlab.com/",
    "CONTRIBUTING",
    "Code of Conduct",
    "/api/execute",
    "/api/workspace",
    "ClassTimeout",
    "error classification",
    "docs/screenshots",
    "[![",
    "LICENSE",
]
hits = []
for item in forbidden:
    if item in text:
        hits.append(item)
if "![ " in text or text.startswith("![") or "\n![" in text:
    hits.append("markdown image")
if "go install" in text:
    hits.append("go install")
assert "没有 `--workspace`" in text
assert "git clone" not in text
assert not hits, hits
print("forbidden content OK")
PY
```

Expected: `forbidden content OK`。若失败，只改 `README.md`，不要改 Go/前端代码。

- [ ] **Step 4: 确认 CLI 与构建命令和代码一致**

Run:

```bash
grep -n 'fsFlag.String("listen"' cmd/httptest/main.go
grep -n 'fsFlag.Bool("open"' cmd/httptest/main.go
grep -n 'defaultListen' cmd/httptest/main.go | head
grep -n 'outDir' web/vite.config.ts
go build -o /tmp/httptest-readme-check ./cmd/httptest
```

Expected:

- `listen` 默认 `"127.0.0.1:1370"`（`const defaultListen`）
- 仅这两个 flag
- `outDir: "../internal/server/ui"`
- `go build` 退出码 0

- [ ] **Step 5: 对照现有测试，确认 YAML 示例字段合法**

Run:

```bash
grep -n 'type Request struct' -A 12 internal/workspace/request.go
grep -n 'type Environment struct' -A 8 internal/workspace/env.go
grep -n 'BodyJSON\|content' internal/workspace/body.go | head
```

Expected: Request 含 `name/method/url/query/headers/body/timeout`；Environment 含 `name/variables`；Body wire 含 `type` 与 `content`。README 示例不得出现这些字段之外的键。

- [ ] **Step 6: Commit**

```bash
git add README.md
git commit -m "$(cat <<'EOF'
docs: 添加根目录 README。

按设计规格用中文说明启动方式、工作区与开发命令，方便克隆后直接使用。
EOF
)"
```

只暂存 `README.md`。若用户要求不提交，跳过本步并在回复里说明。

---

## Spec coverage (self-review)

| 规格节 | 对应 |
|--------|------|
| §1 目的 / 成功标准 | Task 1 全文 + Step 2–4 |
| §2 约束 | Global Constraints；Step 3 禁项 |
| §3 目录顺序 | Step 2 `expected` 标题列表 |
| §4.1–4.11 各节正文 | Task 1 Step 1 完整 README |
| §5 与代码一致 | Step 4 |
| §6 验收（无自动化测试包） | Step 2–5 的命令，不新增 `*_test.go` |
| §7 范围 | File Structure：只建 `README.md` |
