# httptest UI / UX Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 Web UI 改成 VS Code 式活动栏 + URL 通栏，并加上 GitHub/VS Code 深浅主题、应用内对话框和发送/保存/收起快捷键。

**Architecture:** 纯逻辑（主题、活动栏状态机、快捷键判定）抽到可测模块；壳层拆成 ActivityBar、UrlBar、Dialog；`App` 持有状态并接线；CSS 变量两套主题，`html[data-theme]` 切换。不改 Go API、不加 npm 依赖。

**Tech Stack:** React 18 + TypeScript + Vite + 手写 CSS；测试用现有 Vitest。

## Global Constraints

- 仅改 `web/`（实现后 `npm run build` 更新 `internal/server/ui/`）和 README「界面」一节；不改 `internal/`、`cmd/` 逻辑。
- 不新增 npm 包、不加载 Web 字体、不加 React Testing Library。
- 主题键 `httptest.theme`，值 `light` | `dark` | `system`；缺省/非法/读失败 → `dark`。
- 侧栏开合不持久化；刷新默认展开「集合」。
- 界面中文；协议词保留 Method 名与 Query / Headers / Body / Send / Stop / Save。
- 本次不做：未保存切换提醒、栏宽拖动、密钥面板重做、历史选中高亮、新后端字段。
- 浅色 Send 用 `--accent` 蓝，不用绿。
- 规格：`docs/superpowers/specs/2026-09-03-ui-ux-design.md`。

## File Structure

| 路径 | 职责 |
|------|------|
| `web/src/theme.ts` | 解析/读写/应用 `data-theme` |
| `web/src/theme.test.ts` | 主题解析测试 |
| `web/src/activity.ts` | 活动栏点击与 Ctrl+B 开合 |
| `web/src/activity.test.ts` | 活动栏状态机测试 |
| `web/src/shortcut.ts` | 从键盘事件判定动作 |
| `web/src/shortcut.test.ts` | 快捷键判定测试 |
| `web/src/ActivityBar.tsx` | 48px 常驻图标轨 |
| `web/src/UrlBar.tsx` | Method / URL / Send / Stop / Save |
| `web/src/Dialog.tsx` | 路径输入与删除确认 |
| `web/src/App.tsx` | 布局、状态、对话框、快捷键监听 |
| `web/src/TopBar.tsx` | 增加主题三选一 |
| `web/src/Sidebar.tsx` | 去掉内页签；标题栏 + 空状态 |
| `web/src/RequestEditor.tsx` | 去掉 URL 行与底部按钮 |
| `web/src/ResponsePane.tsx` | 空状态文案 |
| `web/src/main.tsx` | 渲染前 `applyTheme(readStoredTheme())` |
| `web/src/styles.css` | token、壳层、活动栏、对话框 |
| `web/index.html` | `lang="zh-CN"`，`data-theme="dark"` |
| `README.md` | 「界面」一节与新壳层一致 |

---

### Task 1: 主题解析模块

**Files:**
- Create: `web/src/theme.ts`
- Test: `web/src/theme.test.ts`
- Modify: `web/src/main.tsx`

**Interfaces:**
- Consumes: 无
- Produces:
  - `export type ThemePref = "light" | "dark" | "system"`
  - `export const THEME_KEY = "httptest.theme"`
  - `export function parseThemePref(raw: string | null): ThemePref`
  - `export function resolveTheme(pref: ThemePref, system: "light" | "dark"): "light" | "dark"`
  - `export function readStoredTheme(getItem?: () => string | null): ThemePref`
  - `export function writeStoredTheme(pref: ThemePref, setItem?: (value: string) => void): void`
  - `export function applyTheme(pref: ThemePref, root?: HTMLElement): void`

- [ ] **Step 1: Write the failing tests**

Create `web/src/theme.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import {
  applyTheme,
  parseThemePref,
  readStoredTheme,
  resolveTheme,
  writeStoredTheme,
} from "./theme";

describe("parseThemePref", () => {
  it("returns dark when missing or invalid", () => {
    expect(parseThemePref(null)).toBe("dark");
    expect(parseThemePref("")).toBe("dark");
    expect(parseThemePref("nope")).toBe("dark");
  });

  it("accepts light dark system", () => {
    expect(parseThemePref("light")).toBe("light");
    expect(parseThemePref("dark")).toBe("dark");
    expect(parseThemePref("system")).toBe("system");
  });
});

describe("resolveTheme", () => {
  it("uses system preference only when pref is system", () => {
    expect(resolveTheme("system", "light")).toBe("light");
    expect(resolveTheme("system", "dark")).toBe("dark");
    expect(resolveTheme("light", "dark")).toBe("light");
    expect(resolveTheme("dark", "light")).toBe("dark");
  });
});

describe("readStoredTheme", () => {
  it("returns dark when getter throws", () => {
    expect(
      readStoredTheme(() => {
        throw new Error("blocked");
      }),
    ).toBe("dark");
  });

  it("parses stored value", () => {
    expect(readStoredTheme(() => "system")).toBe("system");
  });
});

describe("writeStoredTheme", () => {
  it("swallows setter errors", () => {
    expect(() =>
      writeStoredTheme("light", () => {
        throw new Error("quota");
      }),
    ).not.toThrow();
  });
});

describe("applyTheme", () => {
  it("sets data-theme on the root element", () => {
    const el = document.createElement("html");
    applyTheme("system", el);
    expect(el.getAttribute("data-theme")).toBe("system");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd web && npx vitest run src/theme.test.ts`

Expected: FAIL，无法解析 `./theme`。

- [ ] **Step 3: Write minimal implementation**

Create `web/src/theme.ts`:

```ts
export type ThemePref = "light" | "dark" | "system";

export const THEME_KEY = "httptest.theme";

export function parseThemePref(raw: string | null): ThemePref {
  if (raw === "light" || raw === "dark" || raw === "system") return raw;
  return "dark";
}

export function resolveTheme(
  pref: ThemePref,
  system: "light" | "dark",
): "light" | "dark" {
  return pref === "system" ? system : pref;
}

export function readStoredTheme(
  getItem: () => string | null = () => localStorage.getItem(THEME_KEY),
): ThemePref {
  try {
    return parseThemePref(getItem());
  } catch {
    return "dark";
  }
}

export function writeStoredTheme(
  pref: ThemePref,
  setItem: (value: string) => void = (value) =>
    localStorage.setItem(THEME_KEY, value),
): void {
  try {
    setItem(pref);
  } catch {
    // 本机工具：存储失败则仅本次会话有效
  }
}

export function applyTheme(
  pref: ThemePref,
  root: HTMLElement = document.documentElement,
): void {
  root.setAttribute("data-theme", pref);
}
```

In `web/src/main.tsx`, add before `createRoot`:

```ts
import { applyTheme, readStoredTheme } from "./theme";

applyTheme(readStoredTheme());
```

Keep existing imports and render.

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd web && npx vitest run src/theme.test.ts`

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add web/src/theme.ts web/src/theme.test.ts web/src/main.tsx
git commit -m "feat: add theme preference parsing and applyTheme."
```

---

### Task 2: 活动栏状态机

**Files:**
- Create: `web/src/activity.ts`
- Test: `web/src/activity.test.ts`

**Interfaces:**
- Consumes: 无
- Produces:
  - `export type LeftView = "collection" | "history"`
  - `export interface LeftState { view: LeftView; panelOpen: boolean }`
  - `export function clickActivityIcon(state: LeftState, icon: LeftView): LeftState`
  - `export function togglePanel(state: LeftState): LeftState`

- [ ] **Step 1: Write the failing tests**

Create `web/src/activity.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { clickActivityIcon, togglePanel, type LeftState } from "./activity";

const openCollection: LeftState = { view: "collection", panelOpen: true };

describe("clickActivityIcon", () => {
  it("collapses when clicking the active icon", () => {
    expect(clickActivityIcon(openCollection, "collection")).toEqual({
      view: "collection",
      panelOpen: false,
    });
  });

  it("reopens the same view when clicking the active icon while collapsed", () => {
    expect(
      clickActivityIcon({ view: "collection", panelOpen: false }, "collection"),
    ).toEqual({ view: "collection", panelOpen: true });
  });

  it("switches view and opens when clicking the other icon", () => {
    expect(clickActivityIcon(openCollection, "history")).toEqual({
      view: "history",
      panelOpen: true,
    });
  });

  it("opens history even if the panel was collapsed on collection", () => {
    expect(
      clickActivityIcon({ view: "collection", panelOpen: false }, "history"),
    ).toEqual({ view: "history", panelOpen: true });
  });
});

describe("togglePanel", () => {
  it("only toggles open, not the view", () => {
    expect(togglePanel(openCollection)).toEqual({
      view: "collection",
      panelOpen: false,
    });
    expect(togglePanel({ view: "history", panelOpen: false })).toEqual({
      view: "history",
      panelOpen: true,
    });
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd web && npx vitest run src/activity.test.ts`

Expected: FAIL，无法解析 `./activity`。

- [ ] **Step 3: Write minimal implementation**

Create `web/src/activity.ts`:

```ts
export type LeftView = "collection" | "history";

export interface LeftState {
  view: LeftView;
  panelOpen: boolean;
}

export function clickActivityIcon(state: LeftState, icon: LeftView): LeftState {
  if (icon === state.view) {
    return { view: state.view, panelOpen: !state.panelOpen };
  }
  return { view: icon, panelOpen: true };
}

export function togglePanel(state: LeftState): LeftState {
  return { ...state, panelOpen: !state.panelOpen };
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd web && npx vitest run src/activity.test.ts`

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add web/src/activity.ts web/src/activity.test.ts
git commit -m "feat: add activity-bar panel state machine."
```

---

### Task 3: 快捷键判定

**Files:**
- Create: `web/src/shortcut.ts`
- Test: `web/src/shortcut.test.ts`

**Interfaces:**
- Consumes: 无
- Produces:
  - `export type Shortcut = "send" | "save" | "toggle-panel" | "escape" | "block-browser-save"`
  - `export function shortcutFromEvent(e: { key: string; ctrlKey: boolean; metaKey: boolean }, opts: { dialogOpen: boolean }): Shortcut | null`

规则（与规格 §5.3 一致）：

- Ctrl 与 Meta 等效。
- `dialogOpen === true`：`Escape` → `"escape"`；Ctrl/Cmd+S → `"block-browser-save"`（禁止浏览器另存为，但不保存请求）；Ctrl+Enter / Ctrl+B 返回 `null`。
- `dialogOpen === false`：Ctrl+Enter → `"send"`；Ctrl+S → `"save"`；Ctrl+B → `"toggle-panel"`；Escape → `"escape"`。
- 无修饰的 Enter → `null`。

- [ ] **Step 1: Write the failing tests**

Create `web/src/shortcut.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { shortcutFromEvent } from "./shortcut";

function ev(
  key: string,
  mods: { ctrl?: boolean; meta?: boolean } = {},
): { key: string; ctrlKey: boolean; metaKey: boolean } {
  return { key, ctrlKey: !!mods.ctrl, metaKey: !!mods.meta };
}

describe("shortcutFromEvent", () => {
  it("treats Ctrl+Enter as send and plain Enter as nothing", () => {
    expect(shortcutFromEvent(ev("Enter", { ctrl: true }), { dialogOpen: false })).toBe(
      "send",
    );
    expect(shortcutFromEvent(ev("Enter"), { dialogOpen: false })).toBeNull();
  });

  it("treats Cmd+S as save", () => {
    expect(shortcutFromEvent(ev("s", { meta: true }), { dialogOpen: false })).toBe(
      "save",
    );
    expect(shortcutFromEvent(ev("S", { ctrl: true }), { dialogOpen: false })).toBe(
      "save",
    );
  });

  it("treats Ctrl+B as toggle-panel", () => {
    expect(shortcutFromEvent(ev("b", { ctrl: true }), { dialogOpen: false })).toBe(
      "toggle-panel",
    );
  });

  it("returns escape when the dialog is closed", () => {
    expect(shortcutFromEvent(ev("Escape"), { dialogOpen: false })).toBe("escape");
  });

  it("suppresses send save and toggle while a dialog is open", () => {
    expect(
      shortcutFromEvent(ev("Enter", { ctrl: true }), { dialogOpen: true }),
    ).toBeNull();
    expect(shortcutFromEvent(ev("b", { ctrl: true }), { dialogOpen: true })).toBeNull();
    expect(shortcutFromEvent(ev("s", { ctrl: true }), { dialogOpen: true })).toBe(
      "block-browser-save",
    );
    expect(shortcutFromEvent(ev("Escape"), { dialogOpen: true })).toBe("escape");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd web && npx vitest run src/shortcut.test.ts`

Expected: FAIL，无法解析 `./shortcut`。

- [ ] **Step 3: Write minimal implementation**

Create `web/src/shortcut.ts`:

```ts
export type Shortcut =
  | "send"
  | "save"
  | "toggle-panel"
  | "escape"
  | "block-browser-save";

export function shortcutFromEvent(
  e: { key: string; ctrlKey: boolean; metaKey: boolean },
  opts: { dialogOpen: boolean },
): Shortcut | null {
  const mod = e.ctrlKey || e.metaKey;
  const key = e.key;

  if (opts.dialogOpen) {
    if (key === "Escape") return "escape";
    if (mod && (key === "s" || key === "S")) return "block-browser-save";
    return null;
  }

  if (key === "Escape") return "escape";
  if (mod && key === "Enter") return "send";
  if (mod && (key === "s" || key === "S")) return "save";
  if (mod && (key === "b" || key === "B")) return "toggle-panel";
  return null;
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd web && npx vitest run src/shortcut.test.ts`

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add web/src/shortcut.ts web/src/shortcut.test.ts
git commit -m "feat: classify editor keyboard shortcuts."
```

---

### Task 4: CSS 变量、index.html、壳层布局

**Files:**
- Modify: `web/src/styles.css`
- Modify: `web/index.html`

**Interfaces:**
- Consumes: 规格 §4 的 token 表与方法色表
- Produces: `:root` 深色默认；`html[data-theme="light"]` 浅色；`html[data-theme="system"]` 在 `prefers-color-scheme: light` 下用浅色 token。布局 class：`.main`（活动栏 + 可选侧栏 + `.work`）、`.activity`、`.work`、`.urlbar`、`.panes`、`.dialog-backdrop`、`.dialog`、`.method-get|post|put|patch|delete|muted`

不要依赖 `prefers-color-scheme` 作用在未设 `system` 的 `:root` 上（浅色操作系统第一次打开仍须深色）。

- [ ] **Step 1: Set document language and default theme**

`web/index.html` 改为：

```html
<!doctype html>
<html lang="zh-CN" data-theme="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>httptest</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 2: Replace `:root` tokens and layout in `web/src/styles.css`**

把文件顶部的 `:root` / `prefers-color-scheme: dark` 整段换成下面两套变量。保留后面已有的 reset、按钮、kv-table、dump 等，但按下列规则改布局相关选择器。

深色写在 `:root` 与 `html[data-theme="dark"]`；浅色写在 `html[data-theme="light"]` 以及：

```css
@media (prefers-color-scheme: light) {
  html[data-theme="system"] {
    /* 与 light 相同的变量 */
  }
}
```

`:root` 值：

```css
:root,
html[data-theme="dark"] {
  color-scheme: dark;
  --bg: #0d1117;
  --surface: #161b22;
  --activity: #010409;
  --ink: #e6edf3;
  --muted: #7d8590;
  --line: #30363d;
  --accent: #1f6feb;
  --accent-ink: #ffffff;
  --ok: #3fb950;
  --warn: #d29922;
  --err: #f85149;
  --method-get: #3fb950;
  --method-post: #58a6ff;
  --method-put: #d29922;
  --method-patch: #a371f7;
  --method-delete: var(--err);
  --method-muted: var(--muted);
  --mono: ui-monospace, "Cascadia Code", "SF Mono", Menlo, Consolas, monospace;
  --sans: system-ui, "Segoe UI", "PingFang SC", "Noto Sans SC", sans-serif;
  font-family: var(--sans);
  color: var(--ink);
  background: var(--bg);
}
```

浅色（`html[data-theme="light"]` 与 system+light media）值：

```css
--bg: #ffffff;
--surface: #f6f8fa;
--activity: #f6f8fa;
--ink: #1f2328;
--muted: #656d76;
--line: #d0d7de;
--accent: #0969da;
--accent-ink: #ffffff;
--ok: #1a7f37;
--warn: #9a6700;
--err: #cf222e;
--method-get: #1a7f37;
--method-post: #0969da;
--method-put: #9a6700;
--method-patch: #8250df;
--method-delete: var(--err);
--method-muted: var(--muted);
color-scheme: light;
```

删除原来的 `@media (prefers-color-scheme: dark)` 覆盖段，避免浅色 OS 在默认 `dark` 时被系统偏好抢走。

布局：

```css
.main {
  flex: 1;
  min-height: 0;
  display: flex;
}

.activity {
  flex: 0 0 48px;
  width: 48px;
  display: flex;
  flex-direction: column;
  align-items: stretch;
  background: var(--activity);
  border-right: 1px solid var(--line);
}

.activity-btn {
  height: 48px;
  border: none;
  border-radius: 0;
  background: transparent;
  color: var(--muted);
  cursor: pointer;
  position: relative;
}

.activity-btn.active {
  color: var(--ink);
  background: var(--surface);
}

.activity-btn.active::before {
  content: "";
  position: absolute;
  left: 0;
  top: 8px;
  bottom: 8px;
  width: 2px;
  border-radius: 1px;
  background: var(--ink);
}

.activity-btn svg {
  width: 22px;
  height: 22px;
  stroke: currentColor;
  fill: none;
  stroke-width: 1.7;
}

.sidebar {
  flex: 0 0 220px;
  width: 220px;
}

.work {
  flex: 1;
  min-width: 0;
  min-height: 0;
  display: flex;
  flex-direction: column;
}

.urlbar {
  display: flex;
  gap: 0.4rem;
  align-items: center;
  padding: 0.5rem 0.6rem;
  background: var(--surface);
  border-bottom: 1px solid var(--line);
}

.panes {
  flex: 1;
  min-height: 0;
  display: grid;
  grid-template-columns: 1fr 1fr;
}

.editor,
.response {
  border-right: 1px solid var(--line);
}

.response {
  border-right: none;
}

.method-get { color: var(--method-get); }
.method-post { color: var(--method-post); }
.method-put { color: var(--method-put); }
.method-patch { color: var(--method-patch); }
.method-delete { color: var(--method-delete); }
.method-muted { color: var(--method-muted); }

.sidebar-head {
  display: flex;
  align-items: center;
  gap: 0.4rem;
  padding: 0.45rem 0.6rem;
  border-bottom: 1px solid var(--line);
  font-size: 0.72rem;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  color: var(--muted);
}

.sidebar-head .btn-icon {
  margin-left: auto;
}

.empty-hint {
  margin: 1rem 0.6rem;
  color: var(--muted);
  font-size: 0.85rem;
}

.theme-switch {
  display: flex;
  gap: 0.25rem;
}

.theme-switch .btn-active {
  border-color: var(--accent);
  color: var(--accent);
}

.dialog-backdrop {
  position: fixed;
  inset: 0;
  background: rgb(1 4 9 / 55%);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 20;
}

.dialog {
  width: min(420px, 92vw);
  padding: 1rem;
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: 8px;
  box-shadow: 0 8px 24px rgb(0 0 0 / 35%);
}

.dialog h2 {
  margin: 0 0 0.5rem;
  font-size: 1rem;
}

.dialog p {
  margin: 0 0 0.75rem;
  color: var(--muted);
  font-size: 0.85rem;
}

.dialog-path {
  width: 100%;
  margin-bottom: 0.75rem;
}

.dialog-actions {
  display: flex;
  justify-content: flex-end;
  gap: 0.4rem;
}

.btn-danger {
  background: var(--err);
  color: #fff;
  border-color: var(--err);
}
```

把原来的 `.main { grid-template-columns: 220px 1fr 1fr; }` 删掉，改用上面的 flex。`.topbar` 背景用 `var(--surface)`。`.btn-primary` 继续用 `--accent`。

- [ ] **Step 3: 视觉烟测命令（实现接线后跑）**

本任务只改 CSS/HTML，现有 UI 可能暂时错位，允许到 Task 6 再对齐。先确认 CSS 无语法问题：文件能被 Vite 加载即可。

- [ ] **Step 4: Commit**

```bash
git add web/src/styles.css web/index.html
git commit -m "style: add GitHub/VS Code theme tokens and shell layout."
```

---

### Task 5: Dialog 组件

**Files:**
- Create: `web/src/Dialog.tsx`

**Interfaces:**
- Consumes: 无新 API
- Produces: `export default function Dialog(props: Props)`

```ts
export type DialogMode =
  | { kind: "path"; title: string; submitLabel: string; error: string | null }
  | { kind: "confirm"; title: string; body: string; submitLabel: string; error: string | null };

interface Props {
  mode: DialogMode;
  onClose: () => void;
  onSubmit: (path?: string) => void;
}
```

行为：点遮罩不关闭；`Escape` 调 `onClose`；路径模式聚焦 input，`Enter` 提交非空 trim 路径；确认模式聚焦取消按钮，主按钮 `btn-danger`。

- [ ] **Step 1: Implement `web/src/Dialog.tsx`**

规格要求测纯函数、不加 RTL，本组件无独立测试文件。实现必须如下（可微调 class，不可改行为）：

```tsx
import { useEffect, useRef, useState } from "react";

export type DialogMode =
  | { kind: "path"; title: string; submitLabel: string; error: string | null }
  | { kind: "confirm"; title: string; body: string; submitLabel: string; error: string | null };

interface Props {
  mode: DialogMode;
  onClose: () => void;
  onSubmit: (path?: string) => void;
}

export default function Dialog({ mode, onClose, onSubmit }: Props) {
  const [path, setPath] = useState("");
  const inputRef = useRef<HTMLInputElement>(null);
  const cancelRef = useRef<HTMLButtonElement>(null);

  useEffect(() => {
    if (mode.kind === "path") inputRef.current?.focus();
    else cancelRef.current?.focus();
  }, [mode.kind]);

  useEffect(() => {
    function onKey(e: KeyboardEvent) {
      if (e.key === "Escape") {
        e.preventDefault();
        onClose();
      }
    }
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, [onClose]);

  function submit() {
    if (mode.kind === "path") {
      const p = path.trim();
      if (!p) return;
      onSubmit(p);
      return;
    }
    onSubmit();
  }

  return (
    <div className="dialog-backdrop" role="presentation">
      <div
        className="dialog"
        role="dialog"
        aria-modal="true"
        aria-labelledby="dialog-title"
        onClick={(e) => e.stopPropagation()}
      >
        <h2 id="dialog-title">{mode.title}</h2>
        {mode.kind === "path" ? (
          <>
            <p>相对 collections/，例如 auth/ping</p>
            <input
              ref={inputRef}
              className="dialog-path"
              value={path}
              onChange={(e) => setPath(e.target.value)}
              onKeyDown={(e) => {
                if (e.key === "Enter") {
                  e.preventDefault();
                  submit();
                }
              }}
            />
          </>
        ) : (
          <p>{mode.body}</p>
        )}
        {mode.error ? <p className="error">{mode.error}</p> : null}
        <div className="dialog-actions">
          <button ref={cancelRef} type="button" className="btn" onClick={onClose}>
            取消
          </button>
          <button
            type="button"
            className={mode.kind === "confirm" ? "btn btn-danger" : "btn btn-primary"}
            onClick={submit}
          >
            {mode.submitLabel}
          </button>
        </div>
      </div>
    </div>
  );
}
```

说明文案用规格原文「相对 collections/，例如 `auth/ping`」。上面 JSX 里写成「相对 collections/，例如 auth/ping」即可（不必包 code 标签）。

- [ ] **Step 2: Commit**

```bash
git add web/src/Dialog.tsx
git commit -m "feat: add in-app modal dialog."
```

---

### Task 6: ActivityBar、Sidebar、App 壳层与对话框接线

**Files:**
- Create: `web/src/ActivityBar.tsx`
- Modify: `web/src/Sidebar.tsx`
- Modify: `web/src/App.tsx`

**Interfaces:**
- Consumes: `clickActivityIcon`, `togglePanel`, `LeftView`, `LeftState` from `./activity`；`Dialog`
- Produces: App 使用 `left: LeftState`（默认 `{ view: "collection", panelOpen: true }`）；`dialog` 状态；`onNewRequest` / `onDeleteRequest` / `onSave` 不再调用 `window.prompt` / `window.confirm`

- [ ] **Step 1: Create `web/src/ActivityBar.tsx`**

```tsx
import type { LeftView } from "./activity";

interface Props {
  view: LeftView;
  panelOpen: boolean;
  onClickIcon: (view: LeftView) => void;
}

export default function ActivityBar({ view, panelOpen, onClickIcon }: Props) {
  return (
    <nav className="activity" aria-label="侧栏">
      <button
        type="button"
        className={view === "collection" && panelOpen ? "activity-btn active" : "activity-btn"}
        title="集合 (Ctrl+B)"
        aria-label="集合"
        aria-pressed={view === "collection" && panelOpen}
        onClick={() => onClickIcon("collection")}
      >
        <svg viewBox="0 0 24 24" aria-hidden="true">
          <path d="M7 4.5h6.2L17.5 8.8V19.5H7z" />
          <path d="M13.2 4.5V8.8H17.5" />
          <path d="M9.2 12.2h6M9.2 15.2h6" />
        </svg>
      </button>
      <button
        type="button"
        className={view === "history" && panelOpen ? "activity-btn active" : "activity-btn"}
        title="历史"
        aria-label="历史"
        aria-pressed={view === "history" && panelOpen}
        onClick={() => onClickIcon("history")}
      >
        <svg viewBox="0 0 24 24" aria-hidden="true">
          <circle cx="12" cy="13" r="6.2" />
          <path d="M12 10.2V13l2 1.4M8.2 5.2 6.5 7.2l2.2 1" />
        </svg>
      </button>
    </nav>
  );
}
```

选中指示条仅在「该视图且树打开」时出现（与 VS Code 一致：收起后图标轨仍在，但无白条）。

- [ ] **Step 2: Update Sidebar**

去掉内部 `集合 | 历史` tabs。Props 改为：

```ts
interface Props {
  requests: RequestMeta[];
  currentPath: string | null;
  view: "collection" | "history";
  sending: boolean;
  onSelectRequest: (path: string) => void;
  onNewRequest: () => void;
  onDeleteRequest: (path: string) => void;
  onSelectHistory: (entry: HistoryEntry) => void;
}
```

集合标题栏：左侧「集合」，右侧 `＋` 调 `onNewRequest`。`requests.length === 0` 时显示「还没有请求」和按钮「新建请求」。历史为空时显示「发送成功的请求会列在这里」。历史列表的 `useEffect` 依赖改为 `[view, sending]`，且仅当 `view === "history"` 时拉取。删除按钮仍调 `onDeleteRequest`，不再 `window.confirm`。

树项 Method 颜色：在 `req-path` 旁加 `<span className={"method-" + tone(meta)}>`。Sidebar 目前只有 `n.name`。`RequestMeta` 只有 path/name，没有 method。规格要求树项显示方法色。`workspace.requests` 的 API 若无 method，则树项只着色不了方法——**保持现状只显示名称**，方法色用在 UrlBar 的 Method 下拉。不要为方法色改 Go API。

- [ ] **Step 3: Wire App**

增加：

```ts
import { clickActivityIcon, togglePanel, type LeftState } from "./activity";
import ActivityBar from "./ActivityBar";
import Dialog, { type DialogMode } from "./Dialog";

const [left, setLeft] = useState<LeftState>({
  view: "collection",
  panelOpen: true,
});
const [dialog, setDialog] = useState<DialogMode | null>(null);
const [dialogError, setDialogError] = useState<string | null>(null);
```

`onNewRequest`：`setDialogError(null); setDialog({ kind: "path", title: "请求路径", submitLabel: "创建", error: null });`

`onDeleteRequest(path)`：`setDialog({ kind: "confirm", title: "删除请求", body: \`删除 \${path}？此操作会从磁盘去掉该文件。\`, submitLabel: "删除", error: null });` 并记住待删 path（用 `useRef` 或把 path 放进 mode：扩展 confirm 为 `{ kind: "confirm"; path: string; ... }`）。实现时把 `path` 放进 DialogMode：

```ts
| { kind: "confirm"; title: string; body: string; submitLabel: string; error: string | null; path: string }
```

同步改 Task 5 的类型：确认模式带 `path`。若 Task 5 已按无 path 实现，本任务把 `path` 加进 confirm。

`onSave`：若无 `currentPath`，打开路径对话框，`submitLabel: "保存"`，`title: "请求路径"`；提交后走现有 `putRequest`。有路径则直接保存。对话框失败：`setDialogError`，并把 `error` 写进当前 mode；成功关闭 dialog。不要 `setError` banner。

`main` 结构：

```tsx
<div className="main">
  <ActivityBar
    view={left.view}
    panelOpen={left.panelOpen}
    onClickIcon={(icon) => setLeft((s) => clickActivityIcon(s, icon))}
  />
  {left.panelOpen && (
    <Sidebar
      view={left.view}
      /* 其余 props */
    />
  )}
  <div className="work">
    {/* Task 7 放入 UrlBar 与 panes；本任务可暂留 RequestEditor+ResponsePane 并排 */}
    <div className="panes">
      <RequestEditor ... />
      <ErrorBoundary><ResponsePane result={result} /></ErrorBoundary>
    </div>
  </div>
</div>
{dialog && (
  <Dialog
    mode={{ ...dialog, error: dialogError }}
    onClose={() => { setDialog(null); setDialogError(null); }}
    onSubmit={async (path) => { /* 创建/保存/删除 */ }}
  />
)}
```

创建逻辑沿用现有 `onNewRequest` 里 `createRequest` 那段，改为使用对话框给出的 path。

- [ ] **Step 4: Run frontend tests**

Run: `cd web && npm test`

Expected: 现有测试 + Task 1–3 全部 PASS。

- [ ] **Step 5: Commit**

```bash
git add web/src/ActivityBar.tsx web/src/Sidebar.tsx web/src/App.tsx web/src/Dialog.tsx
git commit -m "feat: add activity bar and replace native prompt dialogs."
```

---

### Task 7: UrlBar、RequestEditor、快捷键、空状态

**Files:**
- Create: `web/src/UrlBar.tsx`
- Modify: `web/src/RequestEditor.tsx`
- Modify: `web/src/ResponsePane.tsx`
- Modify: `web/src/App.tsx`
- Modify: `web/src/TopBar.tsx`

**Interfaces:**
- Consumes: `shortcutFromEvent`；`HttpRequest`；`ThemePref` 读写
- Produces: UrlBar 横跨 panes 上方；RequestEditor 无 URL 行、无底部 Send；App `keydown`；TopBar 主题三选一

- [ ] **Step 1: Create `web/src/UrlBar.tsx`**

从 `RequestEditor` 移入 METHODS 常量、method select、url input、dirty、Send/Stop/Save。Method `<select className={"method method-" + methodClass(draft.method)}>`，其中：

```ts
export function methodClass(method: string): string {
  switch (method.toUpperCase()) {
    case "GET": return "get";
    case "POST": return "post";
    case "PUT": return "put";
    case "PATCH": return "patch";
    case "DELETE": return "delete";
    default: return "muted";
  }
}
```

把 `methodClass` 放在 `web/src/activity.ts` 会职责不清。放在 `UrlBar.tsx` 并 export，或放 `web/src/theme.ts` 也不对。新建太碎。就放在 `UrlBar.tsx` 并 export，Sidebar 若不用则仅 UrlBar 使用。

Props：`draft`, `dirty`, `sending`, `saving`, `onChange`, `onSend`, `onStop`, `onSave`。

- [ ] **Step 2: Strip RequestEditor chrome**

删除 `.editor-line` 与 `.editor-actions`。保留 tabs 与 body。Props 去掉 `dirty/sending/saving/onSend/onStop/onSave`，只留 `draft` + `onChange`。

- [ ] **Step 3: Response empty copy**

`response-empty` 文案改为：`填 URL，按 Send 或 Ctrl+Enter`

- [ ] **Step 4: App layout and shortcuts**

`.work` 内顺序：`<UrlBar ... />` 然后 `<div className="panes">` 编辑器 | 响应。

```ts
useEffect(() => {
  function onKey(e: KeyboardEvent) {
    const action = shortcutFromEvent(e, { dialogOpen: dialog !== null });
    if (!action) return;
    if (action === "save" || action === "block-browser-save") e.preventDefault();
    if (action === "send") {
      e.preventDefault();
      if (!sending) void onSend();
    } else if (action === "save") {
      void onSave();
    } else if (action === "toggle-panel") {
      e.preventDefault();
      setLeft((s) => togglePanel(s));
    } else if (action === "escape") {
      if (dialog) {
        setDialog(null);
        setDialogError(null);
      } else if (sending) {
        void onStop();
      }
    }
  }
  window.addEventListener("keydown", onKey);
  return () => window.removeEventListener("keydown", onKey);
}, [dialog, sending, /* stable handlers */]);
```

注意 Dialog 自己也监听 Escape。App 与 Dialog 会各关一次，可接受；或从 Dialog 去掉 window listener、只留 App。**只在 App 处理 Escape**，Dialog 删除自己的 window keydown，避免双触发。改 `Dialog.tsx`：删除 Escape 的 `useEffect`。

`onSend`/`onSave`/`onStop` 需 `useCallback`，否则 effect 过期。

- [ ] **Step 5: TopBar theme switch**

Props 增加 `theme: ThemePref` 与 `onThemeChange: (pref: ThemePref) => void`。在环境选择器左侧放三个按钮：浅色 / 深色 / 系统，当前 `btn-active`。

App：

```ts
const [theme, setTheme] = useState<ThemePref>(() => readStoredTheme());

function onThemeChange(pref: ThemePref) {
  setTheme(pref);
  writeStoredTheme(pref);
  applyTheme(pref);
}
```

- [ ] **Step 6: Run tests**

Run: `cd web && npm test`

Expected: PASS。

- [ ] **Step 7: Commit**

```bash
git add web/src/UrlBar.tsx web/src/RequestEditor.tsx web/src/ResponsePane.tsx web/src/App.tsx web/src/TopBar.tsx web/src/Dialog.tsx
git commit -m "feat: move Send to URL bar and add theme plus shortcuts."
```

---

### Task 8: README、嵌入资源、全量测试

**Files:**
- Modify: `README.md`（「界面」一节）
- Modify: `internal/server/ui/`（`cd web && npm run build` 产物）
- Modify: `docs/superpowers/specs/2026-09-03-ui-ux-design.md` 状态行改为已实现（可选，若规格惯例不改状态则跳过）

- [ ] **Step 1: Update README 界面节**

将「界面」那段换成与规格壳层一致，例如：

顶栏是产品名、工作区路径、浅色/深色/系统、环境选择器和密钥。左侧活动栏切换集合与历史；再点当前图标收起树（`Ctrl+B`）。树展开时宽 220px。Method、URL、Send、Stop、Save 横跨中间请求栏和右侧响应栏。请求栏为 `Query | Headers | Body`；响应栏为状态码、耗时、体积，以及 `Body | Headers | Raw | Timeline`。新建/删除在应用内对话框完成，不使用浏览器原生弹窗。

- [ ] **Step 2: Build embed**

Run: `cd web && npm run build`

Expected: `tsc --noEmit` 通过，产物写入 `internal/server/ui/`。

- [ ] **Step 3: Run all tests**

Run:

```bash
cd web && npm test
cd .. && go test ./...
```

Expected: 全部 PASS。Go 测试与改 UI 前一致（不改 Go 逻辑）。

- [ ] **Step 4: Commit**

```bash
git add README.md web/ internal/server/ui/
git commit -m "feat: ship UI shell redesign in the embedded frontend."
```

---

## Self-review vs spec

| 规格条目 | 任务 |
|---------|------|
| 深浅主题 + system + localStorage + 默认 dark | Task 1, 4, 7 |
| 活动栏 + 可收起树 + URL 通栏 | Task 2, 4, 6, 7 |
| 应用内新建/删除/无路径保存 | Task 5, 6 |
| Ctrl+Enter / S / B，Esc | Task 3, 7 |
| 空状态文案 | Task 6, 7 |
| 方法色 | Task 4, 7（树无 method 字段则仅 UrlBar） |
| 不改 Go API、不加依赖 | 全局 |
| README + embed build | Task 8 |
| 主题/活动栏/快捷键纯函数测试 | Task 1–3 |

树项方法色：workspace API 无 method，规格写「用在树项和 UrlBar」。不扩 API，仅 UrlBar，符合「不改 Go」。
